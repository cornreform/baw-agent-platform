# BAW Codebase Index

Auto-generated by `tools/codebase_doc.py`. AI agents should read this file first before modifying BAW's own code.

- Modules scanned: 121
- Total lines: 40364
- Dependencies tracked: 81
- Circular dependencies: 0

## Module Inventory

| Module | Lines | Docstring | Exports |
|--------|-------|-----------|---------|
| `core/__init__.py` | 1 | BAW — Black And White Agent Platform Core | `` |
| `core/adversarial.py` | 353 | BAW — Adversarial Court: Dual-Soul Architecture | `DevilVoice, AngelVoice, AdversarialCourt` |
| `core/autosave.py` | 129 | BAW — Auto-Save & Version Control Integration | `auto_commit, auto_push, get_last_commit, get_commit_log` |
| `core/backup.py` | 183 | P4: Backup System — daily auto-backup + restore command. | `create_backup, restore_backup, list_backups, pre_upgrade_snapshot, _cleanup_old_` |
| `core/board.py` | 305 | BAW — Dashboard Generator | `_read_task_status, _read_activity_log, _collect_recent_activity, _collect_recent` |
| `core/capabilities.py` | 277 | BAW — Capability Router | `parse_model_overrides, resolve_capability, _find_model, apply_overrides_to_confi` |
| `core/checkpoint.py` | 93 | BAW — Checkpoint System | `Checkpoint, Checkpointer, _deep_copy_messages` |
| `core/commands.py` | 935 | BAW — Slash Commands (P1) | `_cached, _cache_invalidate, set_last_run, handle_slash, _cmd_help, _cmd_version,` |
| `core/commands_capability.py` | 87 | BAW — Capability CLI Commands | `handle_capability_command` |
| `core/config.py` | 283 | Unified BAW config loader (P1-3, Opus 4.8 audit). | `allow_config_write, check_config_write_guard, _deep_merge, _load_yaml, _load_env` |
| `core/context.py` | 258 | BAW — Conversation Context | `Message, Context` |
| `core/court.py` | 761 | BAW Court — Black & White Court state machine (M1 milestone, Fable 5 spec). | `CourtTier, CourtState, Verdict, build_stay_inline_keyboard, CourtCase, _resolve_` |
| `core/data_sources.py` | 218 | BAW — Default Data Source Registry. | `DataSource, get, all_categories, free_sources, summary_block, validate` |
| `core/degradation.py` | 162 | BAW — Tool Degradation (P1) | `register_chain, get_chain, next_strategy, _bash_retry_with_timeout, _bash_retry_` |
| `core/diagnostics.py` | 91 | BAW — Diagnostics (system info for debugging) | `cmd_diagnostics` |
| `core/display.py` | 114 | BAW — Ultra-Concise Display Formatter | `phase_plan, _shorten, phase_step_done, phase_step_running, phase_step_skip, phas` |
| `core/docket.py` | 270 | M4: Court docket — per-user concurrent case queue. | `Priority, DocketEntry, _DocketLock, _load_snapshot, _save_snapshot, _append_log,` |
| `core/docs_chain.py` | 129 | BAW — Docs Chain Reader | `find_docs_chain, read_docs_chain, inject_docs_context` |
| `core/doctor.py` | 213 | BAW — Health Check (doctor) | `C, _ok, _warn, _fail, _header, cmd_doctor` |
| `core/dream.py` | 207 | BAW — Dreaming: On-Hold Task Checker + Light Memory Curation | `dream, _write_dream_log` |
| `core/evolve.py` | 1727 | BAW — Self-Evolution Engine | `_depth_guard, _data_dir, _log_dir, track_tool_call, track_user_feedback, flush_b` |
| `core/exception_tracker.py` | 67 | BAW — Exception tracker for health monitoring. | `record_exception, count_recent, summary` |
| `core/fact_checker.py` | 242 | BAW — Fact Checker with LLM-based Web Search Verification (P0) | `has_source, extract_claims, needs_verification, FactChecker` |
| `core/file_history.py` | 195 | BAW — File History & Version Tracker | `FileHistory` |
| `core/github.py` | 263 | BAW — GitHub Integration | `_check_gh_auth, _get_token, _run_gh, list_repos, _fallback_repos, list_issues, c` |
| `core/guards.py` | 254 | BAW — System-level guards (kimi-k2.6 + deepseek-v4-pro suggestions) | `bail, SessionGuard, execute_tool_with_timeout, safe_stream_content, check_memory` |
| `core/health_dashboard.py` | 411 | P3: Health Dashboard — 10-point system health check. | `health_check, doctor_fix, _attempt_fix, format_doctor_report, format_health_repo` |
| `core/key_vault.py` | 234 | BAW — Secure API Key Vault | `_ensure_vault_dir, _read_env, _write_env, _load_vault, backup, restore, verify, ` |
| `core/learn.py` | 247 | BAW — Self-Learning Skill Acquisition | `learn_from_description, _extract_yaml, learn_from_url` |
| `core/llm.py` | 1084 | BAW — LLM Provider Abstraction Layer | `_get_http_client, _is_fatal_error, _is_quota_error, _is_provider_blacklisted, _b` |
| `core/loop.py` | 2162 | BAW — Agent Loop: Debate-first, Execute-second | `_human_tokens, _classify_task_type, set_context_window, context_window_summary, ` |
| `core/managed_config.py` | 276 | Managed config layer — system-defined overrides that always win. | `_resolve_key, _set_key, _delete_key, is_managed, refuse_write, apply_managed_lay` |
| `core/memory.py` | 563 | BAW — Unified Memory Store | `MemoryStore` |
| `core/memory_curator.py` | 354 | BAW Memory Curator — think before you remember. | `Classification, classify, value_score, detect_conflicts, _detect_contradiction, ` |
| `core/messaging/__init__.py` | 2765 | BAW — Messaging Platform Connector Framework | `Message, ConnectorDef, register, list_connectors, get_connector, BaseConnector` |
| `core/messaging/discord.py` | 220 | BAW — Discord Bot Connector | `DiscordConnector` |
| `core/messaging/matrix.py` | 172 | BAW — Matrix Protocol Connector | `MatrixConnector` |
| `core/messaging/signal.py` | 80 | BAW — Signal Messenger Connector | `SignalConnector` |
| `core/messaging/slack.py` | 288 | BAW — Slack Connector (Socket Mode) | `SlackConnector` |
| `core/messaging/telegram.py` | 2135 | BAW — Telegram Bot Connector | `TelegramConnector` |
| `core/messaging/whatsapp.py` | 99 | BAW — WhatsApp Business Platform Connector | `WhatsAppConnector` |
| `core/model_discovery.py` | 581 | BAW — Model Auto-Discovery | `_find_env_key, guess_provider, auto_discover_model, _guess_capabilities, auto_di` |
| `core/monitor.py` | 171 | P5: Monitoring — Error rate tracker + weekly reliability report. | `track_error, get_error_rate, get_health_score_history, record_health_score, gene` |
| `core/night_court.py` | 94 | M4: 巡迴法庭夜報 — nightly summary of all court activity. | `format_nightly_summary` |
| `core/output_validator.py` | 401 | BAW Output Validator — unified pre-delivery quality gate. | `validate_output, _strip_html, _compress_blank_lines, _markdown_bold_to_html, _re` |
| `core/paths.py` | 164 | BAW — Canonical Path Resolver | `repo_root, runtime_home, data_dir, docs_dir, skills_dir, tools_dir, cli_dir, con` |
| `core/permission.py` | 97 | BAW — Risk-based Permission Engine | `PermissionEngine` |
| `core/permissions.py` | 150 | BAW Permission System | `_perm_path, _load, _save, get_level, check, grant, block, reset, list_perms, _pa` |
| `core/preflight.py` | 241 | [PARSE ERROR] unterminated string literal (detected at line 91) (preflight.py... | `` |
| `core/profile.py` | 100 | BAW — Profile Management | `_profiles_dir, _active_symlink, cmd_profile_list, cmd_profile_create, cmd_profil` |
| `core/render.py` | 145 | BAW — HTML Report Renderer | `bold, italic, code, pre, link, blockquote, spoiler, _e, render_adversarial_court` |
| `core/router.py` | 413 | Task complexity scoring & tier-based routing. | `score_complexity, tier_of, get_tier_preferences, pick_model_for_tier, _get_avail` |
| `core/scheduler.py` | 644 | BAW — Internal Scheduler | `_sanitized_env, ScheduledTask, Scheduler` |
| `core/search.py` | 159 | BAW — Search Provider Registry | `SearchResult, register_search_provider, get_provider, list_providers, search, ge` |
| `core/setup.py` | 1126 | BAW — Interactive CLI Setup & Config | `C, load_config, save_config, _print_header, _print_section, _print_item, _print_` |
| `core/skills.py` | 192 | BAW — Skills System | `Skill, SkillRegistry` |
| `core/system_defaults.py` | 183 | BAW — System Defaults (single source of truth for cross-cutting config). | `SystemDefault, summary_block, validate` |
| `core/task_manager.py` | 299 | BAW — Async Task Manager | `AsyncTask, TaskManager` |
| `core/test_runner.py` | 260 | BAW Telegram Test Runner — Run tests directly from chat. | `run_all_tests, run_unit_tests, run_quick_check, test_config, test_evolve, test_m` |
| `core/todo_state.py` | 206 | BAW — Persistent Todo / Thought / Follow-up Store | `TodoItem, TodoState, get_default_session_id, get_state` |
| `core/token_killer.py` | 499 | BAW Token Killer — systemic tool output compression layer. | `_filter_ansi, _collapse_empty_lines, _deduplicate_lines, _truncate_smart, _extra` |
| `core/tone.py` | 81 | BAW — Tone Switcher (P0) | `detect_tone_switch, format_tone_confirmation` |
| `core/tool_policy.py` | 186 | Per-tool error policies for BAW. | `classify_error, get_max_retries` |
| `core/tool_schema.py` | 241 | BAW — TOOL_DEF schema validator + canonical shape. | `ToolSchemaError, validate_tool_def, validate_tool_def_file, validate_all_tools` |
| `core/tools.py` | 169 | BAW — Tool Registry | `ToolDef, register, get_tool, list_tools, get_openai_tools, execute_tool, clear` |
| `core/tribunal.py` | 447 | Tribunal — Multi-Model Consensus Engine for BAW | `JudgeVerdict, TribunalRuling, _load_baw_config, _get_tribunal_config, _get_defau` |
| `core/tts.py` | 121 | BAW — Text-to-Speech Module | `_get_minimax_t2a_url, minimax_tts, save_audio_bytes` |
| `core/update.py` | 96 | BAW — Self-Update from Git + Version Info | `_get_version, _run, cmd_version, cmd_update` |
| `core/validator.py` | 321 | Real-World Validator — Smoke tests using ACTUAL execution | `ValidationResult, RealWorldValidator, format_results, validate_command` |
| `core/verdict_cache.py` | 81 | M3-7: Verdict cache — skip prosecutor when same goal was already APPROVED. | `_char_bigrams, jaccard, find_reusable_verdict` |
| `core/verifier.py` | 108 | BAW — Step Verifier (P1) | `verify_step, _truncate` |
| `core/watchdog.py` | 221 | P2: Auto-Recovery — Stuck Step Killer + Health Watchdog. | `mark_step_start, mark_step_done, check_stuck, kill_stuck_step, reset_kill_signal` |
| `tools/__init__.py` | 68 | BAW built-in: register all built-in tools | `register_all` |
| `tools/background.py` | 294 | BAW — Background Command Management | `_next_id, start_bg, output_bg, stop_bg, list_bg, cleanup_done, _dispatcher` |
| `tools/bash.py` | 146 | BAW built-in: bash shell execution | `_extract_paths, _matches_glob, _is_sensitive, bash` |
| `tools/browser.py` | 44 | BAW built-in: browser — web automation (stub). | `browser_navigate, _stub` |
| `tools/code_scan.py` | 273 | BAW built-in: code_scan — systematic security scanner for directories. | `_scan_file, _scan_directory, _build_summary, _handler` |
| `tools/codebase_doc.py` | 911 | BAW built-in: codebase documentation — self-documenting, AI-maintainable. | `_is_baw_import, _relative_to_baw, _extract_module_info, _get_parent_chain, _extr` |
| `tools/config.py` | 488 | BAW built-in: config — safe config.yaml read/write with validation. | `_ensure_backup_dir, _backup_config, _load_cfg, _save_cfg, _navigate, config_get,` |
| `tools/cronjob.py` | 374 | BAW built-in: cronjob — autonomous task scheduling tool. | `_get_cron_timezone, _get_cron_dir, _load_jobs, _save_jobs, _parse_schedule, _app` |
| `tools/delegate_task.py` | 528 | BAW built-in: delegate a task to a sub-agent (MiniMax executor). | `_import_baw, _resolve_executor_model, _get_minimax_config, delegate_task` |
| `tools/docker.py` | 282 | BAW built-in: lifecycle — build, restart, status, logs, cleanup. | `_run, _docker_build, _bare_build, _docker_restart, _bare_restart, _docker_status` |
| `tools/document_structuring.py` | 599 | BAW — Document Structuring Tool | `_get_db, _init_db, SectionNumberTracker, _sanitize_filename, _is_ignored, _extra` |
| `tools/execute_code.py` | 229 | BAW built-in: execute_code — Python code execution with BAW tool access. | `_import_tools, execute_code` |
| `tools/fusion_analyze.py` | 271 | BAW built-in: fusion_analyze — multi-model deliberation. | `_try_model, _synthesize, fusion_analyze, _handler` |
| `tools/get_skill.py` | 128 | BAW built-in: Progressive Disclosure — load skill content on-demand. | `get_skill, list_skills, _dispatcher` |
| `tools/git.py` | 162 | BAW built-in: git — commit, push, pull, status, log. | `_ensure_git, _ensure_configured, _git, _handler` |
| `tools/http_fetch.py` | 259 | BAW built-in: browser-aware HTTP fetch for self-build tasks. | `detect_spa, fetch_with_urllib, fetch_with_requests, http_fetch, _suggest_mirror_` |
| `tools/image_generate.py` | 134 | BAW built-in: image_generate — AI image generation via OpenAI DALL-E. | `image_generate` |
| `tools/install.py` | 180 | BAW built-in: Install packages and CLI tools (npm, pip, apt, etc.). | `install` |
| `tools/kg_curator.py` | 209 | BAW built-in: Knowledge Graph Curator — reduce noise, amplify signal. | `_load, _save, _noise_relations, stats, curate, _handler` |
| `tools/knowledge_graph.py` | 415 | BAW — Knowledge Graph Memory | `_ensure_store, _load, _save, _normalize, add_triple, query_entity, query_relatio` |
| `tools/list_files.py` | 161 | BAW built-in: list_files — directory listing with mtime sorting. | `_format_size, _format_time, list_files` |
| `tools/mcp.py` | 301 | BAW — MCP Bridge: connect to MCP servers, discover and invoke tools. | `_load_mcp_config, _dispatcher, _list_servers, _list_tools` |
| `tools/memory.py` | 226 | BAW built-in: memory — persistent memory read/write/search. | `_load_store, memory_remember, memory_search, memory_recall, _memory_dispatcher` |
| `tools/memory_quality.py` | 162 | BAW built-in: Memory Quality — composite 'memory loss score'. | `_load_kg, _kg_metrics, _memory_metrics, _compute_loss, quality_report, loss_scor` |
| `tools/mmx.py` | 503 | BAW built-in: MiniMax CLI (mmx) — image, video, speech, music, vision, search. | `_ensure_mmx, _mmx_is_authd, _run_mmx, mmx_text, mmx_image, mmx_speech, mmx_video` |
| `tools/patch.py` | 95 | BAW built-in: patch — find-and-replace file editing. | `patch_file` |
| `tools/petrestaurants.py` | 385 | BAW built-in: petrestaurants — query Hong Kong's first 1,000 pet-friendly | `_parse_source_md, _build_dataset, refresh, _ensure_fresh, _ensure_fresh_with_not` |
| `tools/read_file.py` | 70 | BAW built-in: read file | `_is_sensitive, read_file` |
| `tools/remember.py` | 244 | BAW built-in: lightweight remember/recall tool for quick fact storage. | `_ensure_file, remember_fact, recall_facts, forget_fact, _dispatcher` |
| `tools/resource_monitor.py` | 199 | BAW built-in: resource_monitor — disk, memory, auto-cleanup. | `_run, _size_mb, _disk_report, _stale_sessions, _old_logs, _cleanup, _handler` |
| `tools/restaurant.py` | 308 | BAW built-in: restaurant — find restaurants via OpenStreetMap Overpass API. | `_bbox_query, _run_overpass, _format_addr, _cache_key, _cache_get, _cache_put, _h` |
| `tools/scan_and_adopt.py` | 438 | BAW built-in: scan_and_adopt — discover foreign tools, adapt to BAW. | `_sanitize_name, _run, _scan_directory, _analyze_via_llm, _safety_check, _adopt_f` |
| `tools/search_files.py` | 141 | BAW built-in: search_files — grep/codebase search. | `search_files, _rg_search, _py_search` |
| `tools/self_capabilities.py` | 149 | BAW built-in: self_capabilities — scan and report own capabilities. | `_handler, _scan_tools, _scan_providers, _scan_config, _system_info` |
| `tools/self_diagnose.py` | 239 | BAW built-in: self_diagnose — comprehensive health check. | `_run, _check_container, _check_providers, _check_tools, _check_memory, _check_di` |
| `tools/self_discover.py` | 285 | BAW built-in: self_discover — detect capability gaps, suggest new tools. | `_scan_logs_for_failures, _inventory_tools, _suggest_new_tools, _handler, _calc_h` |
| `tools/self_migrate.py` | 252 | BAW built-in: self_migrate — migrate BAW to a new machine. | `_run, _export_data, _generate_bootstrap, _analyze_target, _handler, _full_migrat` |
| `tools/selftest.py` | 542 | BAW built-in: Self-test suite — run diagnostics without user interaction. | `_check_config, _check_memory, _check_safety, _check_tts, _check_stt, _check_visi` |
| `tools/session_search.py` | 218 | BAW built-in: session_search — FTS5 cross-session search. | `_get_db, _index_session, session_search` |
| `tools/session_synthesis.py` | 490 | BAW built-in: session_synthesis — cross-session pattern synthesis. | `_load_kg, _keywords, _jaccard, _entries_since, _cluster_entries, _cluster_topic,` |
| `tools/skill_import.py` | 373 | BAW built-in: skill_import — convert Hermes/foreign skills to BAW tools. | `_sanitize_name, _parse_hermes_skill, _convert_to_tool_code, _import_file, _regis` |
| `tools/system.py` | 166 | BAW built-in: system — service, cron, log management. | `_run, _service_status, _disk_usage, _cron_status, _handler, _overall_status` |
| `tools/todo.py` | 269 | BAW built-in: todo — task / thought / follow-up management. | `_get_state, set_session, _format_items, todo_write, todo_read, todo_add_thought,` |
| `tools/tool_generate.py` | 319 | BAW built-in: tool_generate — create new tools autonomously. | `_syntax_check, _generate_code, _write_code, _update_init, _smoke_test, _handler` |
| `tools/tts.py` | 279 | BAW built-in: Multi-Provider TTS (MiniMax / Stepfun / Edge TTS) | `_load_config, _get_env, _detect_provider, tts_list_voices, _tts_minimax, _tts_st` |
| `tools/vision.py` | 238 | BAW built-in: Vision (image understanding) — MiniMax API + Stepfun fallback +... | `_load_config, _encode_image, _vision_minimax, _vision_stepfun, _vision_mmx_cli, ` |
| `tools/web_extract.py` | 101 | BAW built-in: web page text extraction via requests + BeautifulSoup | `web_extract` |
| `tools/web_search.py` | 67 | BAW built-in: web search tool (wraps search provider registry) | `web_search` |
| `tools/write_file.py` | 46 | BAW built-in: write file | `write_file` |

## Dependency Map

```
  core/__init__.py  (no BAW deps)
  core/adversarial.py  (no BAW deps)
  core/autosave.py  (no BAW deps)
  core/backup.py  (no BAW deps)
  core/board.py  (no BAW deps)
  core/capabilities.py  (no BAW deps)
  core/checkpoint.py  (no BAW deps)
  core/commands.py  (no BAW deps)
  core/commands_capability.py  -->  core/capabilities.py
  core/config.py  (no BAW deps)
  core/context.py  (no BAW deps)
  core/court.py  -->  tools/delegate_task.py
  core/data_sources.py  (no BAW deps)
  core/degradation.py  (no BAW deps)
  core/diagnostics.py  (no BAW deps)
  core/display.py  (no BAW deps)
  core/docket.py  (no BAW deps)
  core/docs_chain.py  (no BAW deps)
  core/doctor.py  (no BAW deps)
  core/dream.py  (no BAW deps)
  core/evolve.py  -->  core/dream.py, core/llm.py, core/memory.py
  core/exception_tracker.py  (no BAW deps)
  core/fact_checker.py  (no BAW deps)
  core/file_history.py  (no BAW deps)
  core/github.py  (no BAW deps)
  core/guards.py  (no BAW deps)
  core/health_dashboard.py  -->  core/output_validator.py, core/tools.py
  core/key_vault.py  (no BAW deps)
  core/learn.py  (no BAW deps)
  core/llm.py  (no BAW deps)
  core/loop.py  -->  core/memory_curator.py, core/watchdog.py, tools/kg_curator.py
  core/managed_config.py  (no BAW deps)
  core/memory.py  (no BAW deps)
  core/memory_curator.py  (no BAW deps)
  core/messaging/__init__.py  -->  core/backup.py, core/commands_capability.py, core/config.py, core/context.py, core/guards.py, core/health_dashboard.py, core/llm.py, core/loop.py, core/memory.py, core/monitor.py, core/scheduler.py, core/tools.py, core/tribunal.py, core/validator.py, tools/memory.py, tools/read_file.py, tools/selftest.py, tools/write_file.py
  core/messaging/discord.py  (no BAW deps)
  core/messaging/matrix.py  (no BAW deps)
  core/messaging/signal.py  (no BAW deps)
  core/messaging/slack.py  (no BAW deps)
  core/messaging/telegram.py  -->  core/capabilities.py, core/tts.py, tools/selftest.py, tools/vision.py
  core/messaging/whatsapp.py  (no BAW deps)
  core/model_discovery.py  (no BAW deps)
  core/monitor.py  (no BAW deps)
  core/night_court.py  (no BAW deps)
  core/output_validator.py  (no BAW deps)
  core/paths.py  (no BAW deps)
  core/permission.py  (no BAW deps)
  core/permissions.py  (no BAW deps)
  core/preflight.py  (no BAW deps)
  core/profile.py  (no BAW deps)
  core/render.py  (no BAW deps)
  core/router.py  (no BAW deps)
  core/scheduler.py  (no BAW deps)
  core/search.py  (no BAW deps)
  core/setup.py  -->  core/managed_config.py
  core/skills.py  (no BAW deps)
  core/system_defaults.py  (no BAW deps)
  core/task_manager.py  (no BAW deps)
  core/test_runner.py  -->  core/evolve.py, core/scheduler.py, core/watchdog.py
  core/todo_state.py  (no BAW deps)
  core/token_killer.py  (no BAW deps)
  core/tone.py  (no BAW deps)
  core/tool_policy.py  (no BAW deps)
  core/tool_schema.py  -->  core/tools.py
  core/tools.py  (no BAW deps)
  core/tribunal.py  (no BAW deps)
  core/tts.py  (no BAW deps)
  core/update.py  (no BAW deps)
  core/validator.py  -->  core/evolve.py, core/scheduler.py, core/watchdog.py
  core/verdict_cache.py  (no BAW deps)
  core/verifier.py  (no BAW deps)
  core/watchdog.py  (no BAW deps)
  tools/__init__.py  -->  core/tools.py
  tools/background.py  (no BAW deps)
  tools/bash.py  (no BAW deps)
  tools/browser.py  (no BAW deps)
  tools/code_scan.py  (no BAW deps)
  tools/codebase_doc.py  (no BAW deps)
  tools/config.py  -->  core/config.py, core/managed_config.py
  tools/cronjob.py  (no BAW deps)
  tools/delegate_task.py  -->  core/config.py, core/context.py, core/llm.py, core/router.py, core/tools.py, core/verifier.py
  tools/docker.py  (no BAW deps)
  tools/document_structuring.py  -->  core/paths.py
  tools/execute_code.py  -->  core/tools.py
  tools/fusion_analyze.py  -->  core/config.py, core/llm.py
  tools/get_skill.py  (no BAW deps)
  tools/git.py  (no BAW deps)
  tools/http_fetch.py  -->  core/paths.py
  tools/image_generate.py  (no BAW deps)
  tools/install.py  (no BAW deps)
  tools/kg_curator.py  (no BAW deps)
  tools/knowledge_graph.py  (no BAW deps)
  tools/list_files.py  (no BAW deps)
  tools/mcp.py  (no BAW deps)
  tools/memory.py  -->  core/memory.py, core/memory_curator.py
  tools/memory_quality.py  -->  core/memory.py, tools/kg_curator.py
  tools/mmx.py  (no BAW deps)
  tools/patch.py  (no BAW deps)
  tools/petrestaurants.py  -->  core/paths.py
  tools/read_file.py  (no BAW deps)
  tools/remember.py  -->  core/memory_curator.py
  tools/resource_monitor.py  (no BAW deps)
  tools/restaurant.py  -->  core/paths.py, tools/petrestaurants.py
  tools/scan_and_adopt.py  -->  core/llm.py
  tools/search_files.py  (no BAW deps)
  tools/self_capabilities.py  -->  core/tools.py
  tools/self_diagnose.py  -->  core/health_dashboard.py, core/llm.py, core/tools.py
  tools/self_discover.py  -->  core/tools.py, tools/tool_generate.py
  tools/self_migrate.py  (no BAW deps)
  tools/selftest.py  -->  core/context.py, core/memory.py, core/memory_curator.py, tools/bash.py, tools/read_file.py, tools/vision.py
  tools/session_search.py  (no BAW deps)
  tools/session_synthesis.py  -->  core/evolve.py, core/memory.py
  tools/skill_import.py  (no BAW deps)
  tools/system.py  (no BAW deps)
  tools/todo.py  -->  core/todo_state.py
  tools/tool_generate.py  -->  core/llm.py, core/tools.py
  tools/tts.py  (no BAW deps)
  tools/vision.py  (no BAW deps)
  tools/web_extract.py  (no BAW deps)
  tools/web_search.py  -->  core/search.py
  tools/write_file.py  (no BAW deps)
```

## Dependency Groups

### core/

- `core/__init__.py` depends on: (none)
- `core/adversarial.py` depends on: (none)
- `core/autosave.py` depends on: (none)
- `core/backup.py` depends on: (none)
- `core/board.py` depends on: (none)
- `core/capabilities.py` depends on: (none)
- `core/checkpoint.py` depends on: (none)
- `core/commands.py` depends on: (none)
- `core/commands_capability.py` depends on: core/capabilities.py
- `core/config.py` depends on: (none)
- `core/context.py` depends on: (none)
- `core/court.py` depends on: tools/delegate_task.py
- `core/data_sources.py` depends on: (none)
- `core/degradation.py` depends on: (none)
- `core/diagnostics.py` depends on: (none)
- `core/display.py` depends on: (none)
- `core/docket.py` depends on: (none)
- `core/docs_chain.py` depends on: (none)
- `core/doctor.py` depends on: (none)
- `core/dream.py` depends on: (none)
- `core/evolve.py` depends on: core/dream.py, core/llm.py, core/memory.py
- `core/exception_tracker.py` depends on: (none)
- `core/fact_checker.py` depends on: (none)
- `core/file_history.py` depends on: (none)
- `core/github.py` depends on: (none)
- `core/guards.py` depends on: (none)
- `core/health_dashboard.py` depends on: core/output_validator.py, core/tools.py
- `core/key_vault.py` depends on: (none)
- `core/learn.py` depends on: (none)
- `core/llm.py` depends on: (none)
- `core/loop.py` depends on: core/memory_curator.py, core/watchdog.py, tools/kg_curator.py
- `core/managed_config.py` depends on: (none)
- `core/memory.py` depends on: (none)
- `core/memory_curator.py` depends on: (none)
- `core/messaging/__init__.py` depends on: core/backup.py, core/commands_capability.py, core/config.py, core/context.py, core/guards.py, core/health_dashboard.py, core/llm.py, core/loop.py, core/memory.py, core/monitor.py, core/scheduler.py, core/tools.py, core/tribunal.py, core/validator.py, tools/memory.py, tools/read_file.py, tools/selftest.py, tools/write_file.py
- `core/messaging/discord.py` depends on: (none)
- `core/messaging/matrix.py` depends on: (none)
- `core/messaging/signal.py` depends on: (none)
- `core/messaging/slack.py` depends on: (none)
- `core/messaging/telegram.py` depends on: core/capabilities.py, core/tts.py, tools/selftest.py, tools/vision.py
- `core/messaging/whatsapp.py` depends on: (none)
- `core/model_discovery.py` depends on: (none)
- `core/monitor.py` depends on: (none)
- `core/night_court.py` depends on: (none)
- `core/output_validator.py` depends on: (none)
- `core/paths.py` depends on: (none)
- `core/permission.py` depends on: (none)
- `core/permissions.py` depends on: (none)
- `core/preflight.py` depends on: (none)
- `core/profile.py` depends on: (none)
- `core/render.py` depends on: (none)
- `core/router.py` depends on: (none)
- `core/scheduler.py` depends on: (none)
- `core/search.py` depends on: (none)
- `core/setup.py` depends on: core/managed_config.py
- `core/skills.py` depends on: (none)
- `core/system_defaults.py` depends on: (none)
- `core/task_manager.py` depends on: (none)
- `core/test_runner.py` depends on: core/evolve.py, core/scheduler.py, core/watchdog.py
- `core/todo_state.py` depends on: (none)
- `core/token_killer.py` depends on: (none)
- `core/tone.py` depends on: (none)
- `core/tool_policy.py` depends on: (none)
- `core/tool_schema.py` depends on: core/tools.py
- `core/tools.py` depends on: (none)
- `core/tribunal.py` depends on: (none)
- `core/tts.py` depends on: (none)
- `core/update.py` depends on: (none)
- `core/validator.py` depends on: core/evolve.py, core/scheduler.py, core/watchdog.py
- `core/verdict_cache.py` depends on: (none)
- `core/verifier.py` depends on: (none)
- `core/watchdog.py` depends on: (none)

### tools/

- `tools/__init__.py` depends on: core/tools.py
- `tools/background.py` depends on: (none)
- `tools/bash.py` depends on: (none)
- `tools/browser.py` depends on: (none)
- `tools/code_scan.py` depends on: (none)
- `tools/codebase_doc.py` depends on: (none)
- `tools/config.py` depends on: core/config.py, core/managed_config.py
- `tools/cronjob.py` depends on: (none)
- `tools/delegate_task.py` depends on: core/config.py, core/context.py, core/llm.py, core/router.py, core/tools.py, core/verifier.py
- `tools/docker.py` depends on: (none)
- `tools/document_structuring.py` depends on: core/paths.py
- `tools/execute_code.py` depends on: core/tools.py
- `tools/fusion_analyze.py` depends on: core/config.py, core/llm.py
- `tools/get_skill.py` depends on: (none)
- `tools/git.py` depends on: (none)
- `tools/http_fetch.py` depends on: core/paths.py
- `tools/image_generate.py` depends on: (none)
- `tools/install.py` depends on: (none)
- `tools/kg_curator.py` depends on: (none)
- `tools/knowledge_graph.py` depends on: (none)
- `tools/list_files.py` depends on: (none)
- `tools/mcp.py` depends on: (none)
- `tools/memory.py` depends on: core/memory.py, core/memory_curator.py
- `tools/memory_quality.py` depends on: core/memory.py, tools/kg_curator.py
- `tools/mmx.py` depends on: (none)
- `tools/patch.py` depends on: (none)
- `tools/petrestaurants.py` depends on: core/paths.py
- `tools/read_file.py` depends on: (none)
- `tools/remember.py` depends on: core/memory_curator.py
- `tools/resource_monitor.py` depends on: (none)
- `tools/restaurant.py` depends on: core/paths.py, tools/petrestaurants.py
- `tools/scan_and_adopt.py` depends on: core/llm.py
- `tools/search_files.py` depends on: (none)
- `tools/self_capabilities.py` depends on: core/tools.py
- `tools/self_diagnose.py` depends on: core/health_dashboard.py, core/llm.py, core/tools.py
- `tools/self_discover.py` depends on: core/tools.py, tools/tool_generate.py
- `tools/self_migrate.py` depends on: (none)
- `tools/selftest.py` depends on: core/context.py, core/memory.py, core/memory_curator.py, tools/bash.py, tools/read_file.py, tools/vision.py
- `tools/session_search.py` depends on: (none)
- `tools/session_synthesis.py` depends on: core/evolve.py, core/memory.py
- `tools/skill_import.py` depends on: (none)
- `tools/system.py` depends on: (none)
- `tools/todo.py` depends on: core/todo_state.py
- `tools/tool_generate.py` depends on: core/llm.py, core/tools.py
- `tools/tts.py` depends on: (none)
- `tools/vision.py` depends on: (none)
- `tools/web_extract.py` depends on: (none)
- `tools/web_search.py` depends on: core/search.py
- `tools/write_file.py` depends on: (none)

## Circular Dependencies

None found.

## Interface Contracts (Functions with Arguments)

### `core/adversarial.py`

- `DevilVoice.__init__(self, model: 'ModelDef', angel_persona: str, config: Optional[dict])`
- `DevilVoice.speak(self, user_input: str, context: str) -> dict`
  - Call the Devil voice. Text-only, no tools.
- `AngelVoice.__init__(self, model: 'ModelDef', devil_persona: str, config: Optional[dict])`
- `AngelVoice.speak(self, user_input: str, context: str) -> dict`
  - Call the Angel voice. Text-only (no tool access during court).
- `AdversarialCourt.__init__(self, model: 'ModelDef', system_prompt: str, config: Optional[dict], angel_model: Optional['ModelDef'], devil_model: Optional['ModelDef'])`
- `AdversarialCourt.hold_court(self, user_input: str, memory_context: str, merged: bool) -> dict`
  - Run dual-voice analysis.

merged=True (default): single LLM call for both voices — 2x faster.
merged=False: sequential calls (original behavior).
- `AdversarialCourt._hold_court_merged(self, user_input: str, memory_context: str) -> dict`
  - Single LLM call — both voices + neutral synthesis in one response.

Format expected:
[DEVIL: X/10] ... devil analysis ...
[ANGEL: X/10] ... angel analysis ...
[GAP: X] ... agreement level ...
- `AdversarialCourt.synthesize(self, verdict: dict) -> str`
  - Synthesize both voices into a neutral analysis.

### `core/autosave.py`

- `auto_commit(repo_dir: Path | str, message: str, author: str) -> Optional[str]`
  - Auto-commit changes to git repo.

Args:
    repo_dir: Path to git repo
    message: Commit message
    author: Git author string

Returns:
    Commit hash if committed, None if nothing to commit or er
- `auto_push(repo_dir: Path | str) -> bool`
  - Try to push to remote. Returns True if successful or not needed.
- `get_commit_log(repo_dir: Path | str, limit: int) -> list[dict]`
  - Get recent commit history.

### `core/backup.py`

- `restore_backup(date_str: str) -> dict`
  - Restore from a dated backup. date_str format: '2026-06-15' or 'latest'.
Returns {status, files_restored, details}.

### `core/board.py`

- `_read_task_status(task_dir: Path) -> dict`
  - Read a background task's status files.
- `_read_activity_log(data_dir: Path) -> list[dict]`
  - Read recent activity log entries.
- `_collect_recent_activity(data_dir: Path) -> list[dict]`
  - Merge background tasks + activity log into one timeline, newest first.
- `_collect_recent_tasks(data_dir: Path) -> list[dict]`
  - Get recent background tasks, newest first.
- `generate(data_dir: Path | str) -> str`
  - Generate the dashboard HTML and return the file path.

### `core/capabilities.py`

- `parse_model_overrides(prompt: str) -> tuple[str, dict[str, str]]`
  - Extract [key: value] model override tags from a user prompt.

Returns (cleaned_prompt, overrides_dict).
cleaned_prompt has all tags removed.
overrides_dict maps capability name → model_id, e.g. {"chat
- `resolve_capability(config: dict, function: str, overrides: dict[str, str] | None) -> Optional[dict]`
  - Find the model or method for a given function.

In-task overrides take priority over config when provided.

Returns dict with either:
  {'type': 'model', 'id': ..., 'provider': ..., 'base_url': ..., '
- `_find_model(config: dict, model_id: str) -> Optional[dict]`
- `apply_overrides_to_config(config: dict, overrides: dict[str, str]) -> dict`
  - Deep-copy config and inject in-task model overrides into capabilities section.

Returns a NEW config dict — original is untouched so overrides are per-task only.
- `capability_help(config: dict) -> str`
  - Return formatted routing table.
- `validate_capability_health(config: dict) -> list[dict]`
  - Detect and auto-heal capability config drift.

Returns list of {capability, issue, fix_applied} dicts.
Auto-heals: contradictory fields, missing env vars, method drift.
<b>Code-enforced</b> — runs bef
- `_pick_local_fallback(cap_name: str) -> str`
  - Pick the best local fallback method for a capability.
- `_find_provider_for_base_url(config: dict, base_url: str) -> Optional[str]`
  - Find which provider in config matches a given base_url.
- `_env_var_exists(env_var: str) -> bool`
  - Check if an env var exists in os.environ or ~/.baw/.env.

### `core/checkpoint.py`

- `_deep_copy_messages(messages: list) -> list`
  - Deep copy Message objects for checkpoint restore.
- `Checkpointer.__init__(self)`
- `Checkpointer.save(self, messages: list, tool_name: str, tool_args: dict, plan: list) -> Checkpoint`
  - Save a checkpoint before a state-changing operation.
- `Checkpointer.restore(self) -> Optional[Checkpoint]`
  - Restore the most recent checkpoint. Returns the checkpoint or None.
- `Checkpointer.pop_and_restore(self) -> Optional[Checkpoint]`
  - Pop and restore the most recent checkpoint. Returns the checkpoint.
- `Checkpointer.record_attempt(self)`
  - Record an attempt at the current step.
- `Checkpointer.record_strategy(self, strategy: str)`
  - Record the strategy used for the current step.
- `Checkpointer.attempts(self) -> int`
- `Checkpointer.strategies(self) -> list[str]`
- `Checkpointer.commit(self)`
  - Commit the current step (keep the checkpoint, don't restore on fail).
- `Checkpointer.clear(self)`
  - Clear all checkpoints (new goal).

### `core/commands.py`

- `_cached(key: str, fn) -> str`
  - Return cached result if fresh, else compute + cache.
- `_cache_invalidate(key: str | None)`
  - Invalidate cache. key=None clears all.
- `set_last_run(prompt: str, info: dict)`
  - Store the last prompt and court verdict for /rethink and /court.
- `handle_slash(command: str, args: list[str], config: dict, data_dir: Path, verbose: bool) -> Optional[str]`
  - Route a slash command and return a response string.

Returns None if the command doesn't trigger any action.
- `_cmd_version(data_dir: Path) -> str`
- `_cmd_status(config: dict, data_dir: Path) -> str`
- `_cmd_remember(text: str, data_dir: Path) -> str`
- `_cmd_search(query: str, data_dir: Path) -> str`
- `_cmd_model(model_id: str, config: dict) -> str`
- `_cmd_tone(tone: str, config: dict) -> str`
- `_cmd_dream(data_dir: Path) -> str`
- `_cmd_search_provider(args: list[str], data_dir: Path) -> str`
- `_cmd_rethink(args: list[str], config: dict, data_dir: Path, verbose: bool) -> str`
  - Force the model to rethink the last prompt and give alternative results.
- `_cmd_court(arg: str) -> str`
  - Show court case info.

Sub-commands (M2, Fable 5 spec):
  /court            → recent 5 cases (id + verdict + score + elapsed)
  /court <id>       → full case record (prosecutor, defendant, judge, evid
- `_cmd_fresh(args: list[str], config: dict, data_dir: Path, verbose: bool) -> str`
  - Run with no soul, no memories — raw model judgment.
- `_cmd_docs(filepath: str) -> str`
  - Show the docs chain for a given file path.
- `_cmd_update(data_dir: Path) -> str`
  - Standardized update flow:
1. git fetch
2. Compare version (current vs latest tag)
3. Fetch release notes from GitHub for gap versions
4. Show changelog
5. git pull
6. Post-update hooks (migration, dep
- `_cmd_aux_models(config: dict) -> str`
  - Show all auxiliary models (STT, TTS, vision, image_gen, etc.)
- `_cmd_permit(args: list[str], data_dir: Path) -> str`
  - Grant a permission. Usage: /permit <scope> [duration]
- `_cmd_block(args: list[str], data_dir: Path) -> str`
  - Block a permission. Usage: /block <scope> [duration]
- `_cmd_permissions(data_dir: Path) -> str`
  - List all permissions (session + persistent).
- `_cmd_reset_perms(scope: str | None, data_dir: Path) -> str`
  - Reset permission(s) to default (ask).

### `core/commands_capability.py`

- `handle_capability_command(arg: str, baw: dict) -> str`
  - Process /capability subcommands.

### `core/config.py`

- `check_config_write_guard(path: str) -> None`
  - Raise PermissionError if writing to a protected path without user approval.

Checks:
  1. Managed scope — checks user permission, defaults to blocked
  2. Thread-local write-allow flag (user-initiated
- `_deep_merge(base: dict, override: dict) -> dict`
  - Recursively merge override into base. Override wins on leaf conflict.

Lists are replaced wholesale (no list-merge semantics) — a user config
that wants to change one task_rule should rewrite the whol
- `_load_yaml(path: Path) -> dict`
  - Read a YAML file, returning {} on missing or parse error.
- `model_exists(config: dict, model_id: str) -> bool`
  - True if model_id is declared in any provider's models list.

P0-1 depends on this: when the router hands a model_id down, delegate_task
uses this to confirm it can be resolved before falling back sile
- `get_api_key_for_model(config: dict, model_id: str) -> str`
  - Resolve the API key env-var name for a model and return its value.

Returns "" if the model is unknown or the env var is unset. Does NOT
raise — callers handle the empty-key case.

### `core/context.py`

- `Context.add_user(self, content: str)`
- `Context.add_assistant(self, content: str, tool_calls: list[dict] | None, reasoning_content: str | None)`
- `Context.add_tool_result(self, tool_call_id: str, name: str, content: str)`
- `Context.to_openai_messages(self) -> list[dict]`
  - Convert to OpenAI API message format.
- `Context.count_tokens_approx(self) -> int`
  - Approximate token count (4 chars ≈ 1 token).
- `Context.total_chars(self) -> int`
  - Total character count across all messages (excluding system prompt).
- `Context.compact(self, threshold_chars: int, keep_recent_turns: int) -> tuple[int, str, str]`
  - Compact old conversation turns when context exceeds threshold.

Instead of blind dropping (trim), summarizes old turns into compact form:
'[User: <goal>] → [BAW: <result>]'

**Instruction protection**
- `Context.trim(self, max_messages: int) -> int`
  - Trim message history to prevent unbounded session growth.

Keeps system prompt + most recent messages. Strips oldest tool results
first (tool messages), then oldest user+assistant pairs if still neede

### `core/court.py`

- `build_stay_inline_keyboard(case_id: str) -> list[list[dict]]`
  - Build a Telegram-compatible inline_keyboard for a STAY verdict.

Returns a list of rows; each row is a list of button dicts with
"text" and "callback_data". Three buttons laid out in one row.

For non
- `_resolve_models_for_tier(config: dict, tier: CourtTier, caller_model_id: str) -> dict`
  - Pick defendant/judge/prosecutor models for a tier.

Respects caller_model_id if non-empty (P0-1). Otherwise falls back
to router.DEFAULT_TIER_PREFERENCES (P0-3) and config-driven roles.
- `file_case_sync(goal: str, user_id: str, caller_model_id: str, force_tier: Optional[CourtTier]) -> CourtCase`
  - Synchronous entry point. Wraps file_case() in asyncio.run().

Use this from sync call sites (run_agent, CLI chat). The async
file_case() is still available for callers that already have an
event loop 
- `_archive_case(case: CourtCase) -> None`
  - Persist the case to ~/.baw/court/cases/{id}.json for /court <id> lookup.
- `recent_cases(limit: int) -> list[dict]`
  - Return the most recent N cases from the archive.
- `get_case(case_id: str) -> Optional[dict]`
  - Return full case record by ID, or None.
- `render_verdict(case: CourtCase) -> str`
  - Format a court case's verdict for display (Telegram / TUI / CLI).

Uses the 5 templates from Fable 5 spec.
- `CourtCase.add_evidence(self, role: str, content: str) -> None`
  - Record a piece of evidence (prosecutor critique, defendant output, etc.).
- `CourtCase.transition(self, new_state: CourtState) -> None`
  - Move to a new state. Logs the transition for the audit trail.

### `core/data_sources.py`

- `get(category: str) -> Optional[DataSource]`
  - Look up the preferred default for a data category.

### `core/degradation.py`

- `register_chain(tool_name: str, chain: list[dict])`
  - Register a degradation chain for a tool.

Each entry in the chain:
  {
    "name": str,              # Strategy name (for logging)
    "transform": callable,    # (args: dict, result: str) -> dict or 
- `get_chain(tool_name: str) -> list[dict]`
  - Get the degradation chain for a tool. Returns empty list if none.
- `next_strategy(tool_name: str, attempted: list[str], args: dict, result: str) -> Optional[dict]`
  - Find the next untried strategy for a tool.

Args:
    tool_name: Tool that failed
    attempted: List of strategy names already tried
    args: Original tool arguments
    result: Tool failure result 
- `_bash_retry_with_timeout(args: dict, result: str) -> Optional[dict]`
  - If timeout, retry with double the timeout.
- `_bash_retry_with_workdir(args: dict, result: str) -> Optional[dict]`
  - If 'No such file' or path error, try with parent dir.
- `_write_file_retry_path(args: dict, result: str) -> Optional[dict]`
  - If 'Permission denied' or path error, try /tmp.
- `_search_retry_simpler_query(args: dict, result: str) -> Optional[dict]`
  - If search returns nothing, try with shorter query.
- `_search_retry_different_provider(args: dict, result: str) -> Optional[dict]`
  - If search error, keep query but try a note about the error.

### `core/diagnostics.py`

- `cmd_diagnostics(data_dir: Path)`
  - Collect system info for debugging.

### `core/display.py`

- `phase_plan(steps: list[dict]) -> str`
  - Show only overall plan goal, no step structure.
- `_shorten(desc: str, max_len: int) -> str`
  - Shorten a step description.
- `phase_step_done(group: str, step_in_group: int, group_total: int, desc: str, result: str) -> str`
- `phase_step_running(group: str, step_in_group: int, group_total: int, desc: str) -> str`
- `phase_step_skip(group: str, step_in_group: int, group_total: int, desc: str, reason: str) -> str`
- `phase_step_error(group: str, step_in_group: int, group_total: int, desc: str, resolution: str) -> str`
- `done(steps: int, total: int, elapsed: float, cost: float) -> str`
- `court_verdict(verdict: dict) -> str`
- `front_desk_task_id(task_id: str) -> str`
- `front_desk_status(task_id: str, status: str, detail: str) -> str`

### `core/docket.py`

- `_save_snapshot(state: dict) -> None`
  - Persist the docket state atomically (write-temp + rename).
- `_append_log(entry: dict) -> None`
  - Append-only audit log. Not used for state recovery, just history.
- `enqueue(case_id: str, user_id: str, tier: int, goal: str, priority: Priority) -> DocketEntry`
  - Add a case to the docket. Returns the DocketEntry (with status).
- `get_queue_position(case_id: str) -> Optional[int]`
  - Return the queue position (1-indexed) for a queued case, or None if running/done.
- `mark_running(queue_id: str) -> bool`
  - Move a queued entry to running. Returns True if successful.
- `mark_done(queue_id: str, success: bool) -> None`
  - Move a running entry to done/failed.
- `cancel(queue_id: str) -> bool`
  - Cancel a queued or running case. Returns True if cancelled.
- `_DocketLock.__init__(self)`
- `_DocketLock.__enter__(self)`
- `_DocketLock.__exit__(self)`

### `core/docs_chain.py`

- `find_docs_chain(file_path: str, project_root: str | None) -> list[Path]`
  - Find the doc chain for a given file path.

Returns [root_doc, dir_doc, file_doc] (filtered to existing files).
- `read_docs_chain(file_path: str, project_root: str | None) -> str`
  - Read the full docs chain for a file and return as context string.

Returns empty string if no docs found.
- `inject_docs_context(messages: list[dict], file_path: str, project_root: str | None) -> list[dict]`
  - Inject docs chain context into messages before an edit operation.

Returns modified messages list with docs context appended as system message.

### `core/doctor.py`

- `_ok(msg: str)`
- `_warn(msg: str)`
- `_fail(msg: str)`
- `_header(title: str)`
- `cmd_doctor(data_dir: Path, fix: bool)`
  - Run comprehensive health check.

### `core/dream.py`

- `dream(data_dir: Path, dry_run: bool) -> dict`
  - Weekly dream pass.
1. Check for on-hold/stuck tasks (PRIMARY)
2. Light memory curation (secondary, fast path only)
Returns a report dict.
- `_write_dream_log(data_dir: Path, report: dict)`
  - Write human-readable dream log.

### `core/evolve.py`

- `_depth_guard(func)`
  - Prevent recursive self-improvement calls > 3 deep.
- `track_tool_call(name: str, args: dict, success: bool, duration: float, error: str, session_id: str) -> dict`
  - Record a tool call for behavioral analysis.

Thread-safe: appends to in-memory buffer and flushes every 10 entries.
- `track_user_feedback(text: str, session_id: str) -> dict`
  - Record a user message that may contain corrections or feedback.

Detects correction patterns like "唔好", "不要", "錯咗", "改做".
- `_args_signature(args: dict) -> str`
  - Short signature of tool arguments for pattern matching.

Strips dynamic content (file contents, long strings), keeps structure.
- `analyze(hours_back: int) -> dict`
  - Analyze behavior log for patterns.

Returns recommendations for self-improvement.
- `_git_snapshot(tag_reason: str) -> dict`
  - Create a git snapshot before any modifications.

Returns {"ok": bool, "commit": str, "error": str}.
- `_git_rollback(commit_hash: str, reason: str) -> dict`
  - Revert to a previous git snapshot.

Returns {"ok": bool, "error": str}.
- `_verify_soul(path: Path) -> dict`
  - Verify SOUL.md is valid markdown and contains required sections.
- `_verify_config(path: Path) -> dict`
  - Verify config.yaml is valid YAML.
- `_save_pending(pending: list[dict])`
- `queue_for_approval(recommendations: list[dict]) -> list[dict]`
  - Queue auto-optimization recommendations for user approval.

Returns the list of newly queued items.
- `approve_pending(index: int, approved: bool) -> dict`
  - Approve or reject a pending optimization by index.

Returns {"ok": bool, "action": str, "error": str}.
- `auto_optimize(dry_run: bool) -> dict`
  - Run analysis and apply optimizations based on detected patterns.

Safety-first flow:
  1. Git snapshot before any writes (P3-4)
  2. Dry-run collects patches; real run needs explicit --apply (P3-1)
  
- `auto_optimize_single(rec: dict) -> dict`
  - Apply a single approved recommendation (used by approval queue).
- `_save_learned_lessons(lessons: list[dict])`
- `_llm_classify_corrections(correction_texts: list[str]) -> list[dict]`
  - Use LLM to classify corrections beyond keyword matching.

Batches up to 10 corrections into one lightweight LLM call.
Falls back gracefully if LLM is unavailable.
Returns lessons in same format as _ex
- `_extract_correction_lessons(correction_texts: list[str]) -> list[dict]`
  - Extract specific behavioral lessons from user correction texts.

Looks for patterns like:
  - "太長" / "簡短" / " concise" → lesson: response_length=short
  - "用繁體" / "不要簡體" → lesson: language=traditional
- `_write_learned_lessons(new_lessons: list[dict]) -> int`
  - Merge new lessons into learned_lessons.json, deduplicate.
- `_tune_prompt_style(analysis: dict) -> dict`
  - P4-2: Auto-tune prompt style based on user corrections.

Analyzes correction patterns and returns tuning recommendations.
Does NOT modify prompts directly — returns suggestions for approval.
- `_optimize_model_routing(analysis: dict) -> dict`
  - P4-3: Recommend model routing changes based on success rates.

Looks at tool-level success rates and suggests model swaps.
Returns suggestions only — does NOT modify config directly.
- `_detect_behavioral_drift(feedback: list[dict]) -> list[dict]`
  - P4-4: Detect when BAW behavior drifts from SOUL.md identity.

Looks for feedback indicating identity confusion or style drift.
- `_auto_patch_code(analysis: dict) -> dict`
  - D1: Generate and apply code patches based on failure patterns.

Uses LLM to generate patches for tools with high failure rates.
Safety: git snapshot → syntax verify → test → commit/rollback.

Returns 
- `_analyze_memory_for_preferences(days_back: int) -> dict`
  - Read memory store and extract user preferences + behavioral patterns.

Returns structured analysis for SOUL.md revision.
- `_generate_soul_revisions(analysis: dict) -> list[dict]`
  - Generate specific SOUL.md revision suggestions based on memory analysis.

Each suggestion has: section, content, priority
- `_apply_soul_revisions(revisions: list[dict]) -> dict`
  - Apply generated revisions to SOUL.md safely.

Uses git snapshot + rollback on failure.
- `_snapshot_file(path: Path) -> str | None`
  - Capture file content for before/after comparison.
- `_compute_diff(before: str | None, after: str | None, label: str) -> dict`
  - Compute a simple line-level diff between before and after.
- `_save_audit_entry(result: dict, before_files: dict, after_files: dict)`
  - Append an evolution audit entry to JSONL log.
- `get_evolution_history(limit: int) -> list[dict]`
  - Read recent evolution audit entries.
- `format_evolution_diff(entries: list[dict]) -> str`
  - Format evolution audit entries for display.
- `_audited_auto_optimize(dry_run: bool) -> dict`
  - Wrapped auto_optimize with audit trail.

### `core/exception_tracker.py`

- `record_exception(exc: Exception, context: str)`
  - Record an exception to the JSONL log.
- `count_recent(hours: int) -> tuple[int, list[dict]]`
  - Return (count, recent_entries) from last N hours.
- `summary(hours: int) -> str`

### `core/fact_checker.py`

- `has_source(text: str, claim_context: str) -> bool`
- `extract_claims(text: str) -> list[dict]`
  - Extract potential factual claims with context for source checking.
- `needs_verification(task_type: str, mode: str) -> bool`
- `FactChecker.__init__(self, config: dict)`
- `FactChecker.check(self, text: str, task_context: str) -> tuple[str, dict]`
  - First-pass regex-based check.

Returns:
    ("pass", ...) / ("flag", ...) / ("block", ...)
- `FactChecker.verify_with_search(self, text: str, task_context: str) -> tuple[str, dict]`
  - Second-pass verification using web search.

After regex check, for each unverified claim, run web search
to confirm or flag it.

Returns:
    ("pass", ...) — all claims verified via web search
    ("f

### `core/file_history.py`

- `FileHistory.__init__(self, data_dir: Optional[Path])`
- `FileHistory._load_manifest(self)`
  - Load existing manifest entries.
- `FileHistory._save_entry(self, entry: dict)`
  - Append one entry to the manifest.
- `FileHistory._content_hash(self, content: str) -> str`
  - SHA256 of content (first 1KB for quick compare).
- `FileHistory._timestamp(self) -> str`
  - ISO 8601 timestamp with timezone.
- `FileHistory.record_write(self, path: Path | str, content: str, action: str, metadata: dict)`
  - Record a file write with backup of previous version.

Args:
    path: Absolute or relative file path
    content: New content being written
    action: "create" | "update" | "delete"
    metadata: Opt
- `FileHistory.record_delete(self, path: Path | str, metadata: dict)`
  - Record a file deletion.
- `FileHistory.get_history(self, path: Path | str, limit: int) -> list[dict]`
  - Get version history for a specific file, newest first.
- `FileHistory.get_timeline(self, since: str, limit: int) -> list[dict]`
  - Get global timeline of all file changes.

Args:
    since: ISO timestamp, e.g. "2026-06-07T00:00:00Z"
    limit: Max entries
- `FileHistory.latest_version(self, path: Path | str) -> Optional[dict]`
  - Get the latest version entry for a file.
- `FileHistory.stats(self) -> dict`
  - Get file history statistics.
- `FileHistory.to_html(self, limit: int) -> str`
  - Render recent history as HTML.

### `core/github.py`

- `_run_gh(args: list[str], timeout: int) -> dict`
  - Run a gh CLI command and return parsed JSON result.
- `list_repos(owner: str) -> list[dict]`
  - List GitHub repos, optionally filtered by owner.
- `_fallback_repos(owner: str) -> list[dict]`
  - Read-only fallback: list repos from public API.
- `list_issues(repo: str, state: str, label: str) -> list[dict]`
  - List issues in a repo.
- `create_issue(repo: str, title: str, body: str, label: str) -> dict`
  - Create a new issue.
- `close_issue(repo: str, number: int) -> dict`
  - Close an issue.
- `list_prs(repo: str, state: str) -> list[dict]`
  - List pull requests.
- `pr_status(repo: str, number: int) -> dict`
  - Check PR status (CI checks, mergeable state).
- `list_workflows(repo: str) -> list[dict]`
  - List GitHub Actions workflows.
- `list_runs(repo: str, branch: str, limit: int) -> list[dict]`
  - List recent workflow runs.

### `core/guards.py`

- `bail(key: str) -> str`
  - Return a consistent error message. All user-facing errors use this.
- `execute_tool_with_timeout(name: str, args: dict, executor: Callable, timeout: int) -> str`
  - Execute a tool with a timeout guard. Returns result string.
- `safe_stream_content(text: str, max_length: int) -> str`
  - Truncate streamed content if it exceeds safe limit.
- `check_safety(name: str, arguments: dict) -> tuple`
  - Pre-execution safety check. Returns (blocked: bool, reason: str).

Blocks dangerous operations that haven't been audited.
Currently enforces: no silent downloads without audit awareness.
- `SessionGuard.init(cls, save_callback: Callable, interval: int)`
  - Start periodic auto-save. save_callback is called every `interval`.
- `SessionGuard._schedule_next(cls)`
- `SessionGuard._do_save(cls)`
- `SessionGuard.shutdown(cls)`
  - Save once more and stop timer.
- `SessionGuard.save_now(cls)`
  - Force immediate save.

### `core/health_dashboard.py`

- `_attempt_fix(check: dict) -> dict | None`
  - Try to fix a single health check issue. Returns fix record or None.
- `format_doctor_report(result: dict) -> str`
  - Format doctor fix results as readable string.
- `format_health_report(result: dict) -> str`
  - Format health check result as a readable string.

### `core/key_vault.py`

- `_read_env(path: Path) -> dict`
  - Read a .env file and return a dict of key->value pairs.
- `_write_env(path: Path, data: dict)`
  - Write a dict of key->value pairs to a .env file.

### `core/learn.py`

- `learn_from_description(description: str, data_dir: Path, config: dict, verbose: bool, dry_run: bool) -> dict`
  - Take a skill description and autonomously create a BAW skill.

Returns: {
    "name": str,
    "file": str or None,
    "steps": int,
    "analysis": str,
    "yaml": str,
    "error": str or None,
}
- `_extract_yaml(text: str) -> str`
  - Extract YAML content from markdown code blocks or raw text.
- `learn_from_url(url: str, data_dir: Path, config: dict, verbose: bool, dry_run: bool) -> dict`
  - Fetch content from a URL and learn a skill from it.

### `core/llm.py`

- `_is_fatal_error(error_signature: str) -> bool`
  - Check if an error signature indicates a fatal (non-recoverable) error.
- `_is_quota_error(error_signature: str) -> bool`
  - Check if an error signature indicates a quota/rate-limit error.
- `_is_provider_blacklisted(provider: str) -> bool`
  - Check if a provider is permanently blacklisted due to fatal errors.
- `_blacklist_provider(provider: str) -> None`
  - Permanently blacklist a provider for the current session.
- `_is_provider_healthy(provider: str) -> bool`
  - Check if a provider is healthy enough to try.

Returns False if: permanently blacklisted, or circuit open + high failure rate.
- `_check_circuit(provider_or_model: str) -> None`
  - Raise RuntimeError ONLY if circuit just opened (give a grace period).

If circuit is in cooldown, do NOT raise — instead log a warning and
return. The caller will try the call, and on failure the next
- `_record_circuit_failure(provider_or_model: str, error_signature: str) -> None`
  - Record a failure. Opens circuit if threshold reached AND
failure signatures repeat (same error pattern, not transient).

error_signature: short hash of the error class+code so we only
count *persisten
- `_record_circuit_success(provider: str) -> None`
  - Reset circuit on success.
- `_record_fallback(primary: str, fallback: str, reason: str) -> None`
- `_on_shutdown(signum, frame)`
- `calculate_cost(model: ModelDef, input_tokens: int, output_tokens: int) -> float`
  - Calculate cost in USD from token counts + model pricing (per 1M tokens).
- `load_config(config_path: Optional[Path]) -> dict`
  - Load BAW config from yaml or dict, and auto-load .env for API keys.

P1-3 (Opus 4.8 audit): delegates to core.config.load_config so all
callers see the same merged view. Maintained as a thin wrapper f
- `get_model(config: dict, model_id: Optional[str]) -> ModelDef`
  - Resolve a model definition from config. Protocol-agnostic.
- `register_protocol(name: str, handler: Callable)`
  - Register a new protocol handler. Call this to add support for any API format.
- `call_llm(model: ModelDef, messages: list[dict], tools: Optional[list[dict]], temperature: float, max_tokens: Optional[int]) -> LLMResponse`
  - Call LLM via configured protocol. Falls back to openai-chat if protocol unknown.
- `_call_openai_chat(model: ModelDef, messages: list[dict], tools: Optional[list[dict]], temperature: float, max_tokens: Optional[int]) -> LLMResponse`
  - OpenAI-compatible /v1/chat/completions. Works with: DeepSeek, MiniMax, Groq, Together, etc.
- `_call_anthropic(model: ModelDef, messages: list[dict], tools: Optional[list[dict]], temperature: float, max_tokens: Optional[int]) -> LLMResponse`
  - Anthropic Claude /v1/messages format.
- `_call_google(model: ModelDef, messages: list[dict], tools: Optional[list[dict]], temperature: float, max_tokens: Optional[int]) -> LLMResponse`
  - Google Gemini /v1beta/models/*:generateContent.
- `_post(url: str, headers: dict, body: dict) -> dict`
  - HTTP POST with error handling + circuit breaker integration.
- `_record_latency(provider: str, model: str, latency: float, status: str)`
  - Append latency record to JSONL log.
- `call_llm_with_fallback(config: dict, messages: list[dict], tools: Optional[list[dict]], primary_id: Optional[str], temperature: Optional[float], max_tokens: Optional[int]) -> FallbackResult`
  - Call LLM with auto-fallback + exponential backoff retry + circuit breaker.

Tries primary model up to 3 times with exponential backoff (1s→2s→4s)
on transient errors (429, 503, timeout, connection). N
- `_call_with_timeout(model, messages, tools, temperature, max_tokens)`
  - Single LLM call with hard wall-clock timeout.

The shared httpx client has a 120s read timeout, but slow-streaming
servers can reset that timer indefinitely. We wrap the call in a
thread with a hard t
- `_route_model(config, messages, primary_id)`
  - Auto-route based on message size to long-context model.
- `validate_config(config: dict) -> list[str]`
  - Validate config.yaml structure. Returns list of warnings/errors.
- `ping_provider_health(config: dict) -> dict`
  - Check all configured provider API key presence at startup.

Two-phase check:
  Phase 1: Key presence — does the .env var exist?
  Phase 2: HTTP ping to /models — is the provider reachable?
    (Phase 

### `core/loop.py`

- `_human_tokens(n: int) -> str`
  - Format token count as human-readable string.
- `_classify_task_type(prompt: str) -> dict`
  - Classify user task into TYPE_A–TYPE_E BEFORE execution.

TYPE_A: read-only / informational → safe, no audit needed
TYPE_B: write/modify system/config → needs permission gate
TYPE_C: download/install t
- `set_context_window(model_id: str, ctx_window: int, current_pct: float)`
  - Update context window tracking for display. Called by _run_baw() each message.
- `_ctx_bar(pct: float) -> str`
  - 5-block context bar.
- `record_cost(model_name: str, tokens_in: int, tokens_out: int, cost: float)`
- `_summarize_providers(config: dict) -> str`
  - Compact provider summary: 'deepseek(3) minimax(8) openrouter(342) ...'
- `build_system_prompt(config: dict, data_dir: Optional[Path], fresh_start: bool, quick_mode: bool) -> str`
  - Build system prompt from SOUL.md + dynamic context.

quick_mode=True: loads only core identity, skips heavy court/tool rules.
fresh_start=True: returns a minimal prompt with no SOUL.md or memories.

S
- `_build_todo_block(data_dir: Path) -> str`
  - Build the todo-system reminder + carry-over follow-ups block.

Injected at the end of the system prompt so BAW is reminded every turn
to think out loud, track work, and surface pending follow-ups.
- `_verify_post_turn_claims(output: str, data_dir: Optional[Path]) -> str`
  - Verify config change claims in BAW's output against actual config.yaml.

BAW framework prevents fabrication by mediating tool execution.
BAW's post-turn hook catches fabrication after generation.
If B
- `_verify_after_write(name: str, args: dict, result: str, data_dir: Optional[Path]) -> str`
  - Code-enforced verify after write tools. Reads back to confirm changes persist.

Returns original result with verification note appended.
Does NOT block execution — just flags for the LLM to see.
- `run_agent(prompt: str, config: dict, model_id: Optional[str], data_dir: Optional[Path], verbose: bool, interactive: bool, fresh_start: bool, mode: Optional[str], conversation_history: Optional[list[dict]], progress_callback: Optional[Callable[..., None]], max_tool_turns: int) -> tuple[str, dict]`
  - Run BAW agent with debate-first, execute-second flow.

Phase 1 — Court: Devil + Angel analyze independently.
Phase 2 — Neutral response & debate (interactive only).
Phase 3 — Execute: plan → execute →
- `CostTracker.__init__(self)`
- `CostTracker.over_budget(self) -> bool`
  - Check if total tokens exceed budget. Thread-safe.
- `CostTracker.record(self, model_name: str, tokens_in: int, tokens_out: int, cost: float)`
- `CostTracker.summary(self) -> str`
- `CostTracker.html_summary(self) -> str`
- `CostTracker.reset(self)`

### `core/managed_config.py`

- `_resolve_key(config: dict, path: str) -> tuple[dict | None, str]`
  - Walk a dotted path into a nested dict.

Returns (parent_dict, leaf_key).  If an intermediate key is missing
or not a dict, returns (None, '').
- `_set_key(config: dict, path: str, value) -> bool`
  - Set a dotted key path in a nested dict. Creates intermediate dicts.

Returns True if the value was actually different from existing, False if same.
- `_delete_key(config: dict, path: str) -> bool`
  - Delete a dotted key path from a nested dict. Returns True if removed.
- `is_managed(path: str) -> bool`
  - Check if a config path is managed (blocked from agent writes).
- `refuse_write(path: str) -> None`
  - Check permission for a managed key write.

Default: raise PermissionError (ask user).
If user has granted session/permanent permission, allow.
If user has blocked, raise with BLOCKED message.
- `apply_managed_layer(config: dict) -> dict`
  - Re-assert all managed key values onto *config* (in-place + return).

This is the load-time override: whatever the user config says,
managed values win.  Call this as the **last** merge step in
load_co
- `strip_managed_keys(config: dict) -> dict`
  - Remove all managed keys from *config* (deep copy, returns new dict).

Use this before writing config back to disk — prevents the save
from persisting unintended managed values.
- `initialize_managed_source(repo_root: Path | None) -> None`
  - Create ~/.baw/managed_config.yaml from repo config.yaml if missing.

Extracts the current managed keys from the live config and writes
them to the managed source file.  Safe to call on every startup.

### `core/memory.py`

- `MemoryStore.__init__(self, data_dir: Path)`
- `MemoryStore._load(self)`
  - Load all entries from JSONL into cache.
- `MemoryStore._save(self, entry: dict)`
  - Append one entry to JSONL.
- `MemoryStore._save_all(self)`
  - Overwrite store.jsonl with entire cache (using atomic write).
- `MemoryStore._get_edges(self) -> dict`
  - Load edge relationships.
- `MemoryStore._save_edges(self, edges_data: dict)`
  - Persist edges.json.
- `MemoryStore._keywords(text: str) -> set[str]`
  - Extract meaningful keywords from text.

For Latin text: \w+ tokens (filtered for stop words).
For CJK text: 2-char bigrams (each consecutive pair of CJK chars).
Combined into a single keyword set for 
- `MemoryStore._jaccard(a: set, b: set) -> float`
  - Jaccard similarity between two keyword sets.
- `MemoryStore._find_or_create_edge(self, edges: list, source: str, target: str) -> dict | None`
  - Find an existing edge between source and target, or None.
- `MemoryStore._create_edges_for_new_memory(self, mem_id: str, content: str)`
  - Compare new memory with all existing entries and auto-create edges.

An edge is created when two memories share ≥2 keywords,
with weight = Jaccard similarity of their keyword sets.
- `MemoryStore._build_adjacency(self, edges: list[dict]) -> dict[str, list[tuple[str, float]]]`
  - Build adjacency list: {mem_id: [(neighbour_id, weight), ...]}
- `MemoryStore._associative_spread(self, source_id: str, direct_boost: float, indirect_boost: float)`
  - 2-hop score propagation through edges graph.

Original design (BAW-PLAN):
  - Direct neighbours (1-hop): +0.05 × edge_weight
  - Neighbour-of-neighbour (2-hop): +0.02 × edge_weight × 0.5 (decaying)

F
- `MemoryStore.remember(self, content: str, tags: list[str] | None, source: str) -> dict`
  - Store a new memory entry with initial score 0.50.

Auto-creates edges to related memories (keyword-based Jaccard).
Runs associative spread to boost neighbours via the edges graph.

De-duplication: if 
- `MemoryStore.search(self, query: str, limit: int) -> list[dict]`
  - Search memory by keyword similarity (Jaccard), sorted by score (descending).

Also checks substring match for exact hits.
Hit entry gets +0.05 score boost + graph associative spread.
- `MemoryStore.decay(self)`
  - Decay scores for old entries. Call daily. Auto-persists + triggers compression when needed.
- `MemoryStore.stats(self) -> dict`
  - Return memory statistics, including edge count.
- `MemoryStore.edge_stats(self) -> str`
  - Return human-readable edge graph summary.
- `MemoryStore.compress_old(self, max_age_days: int, min_score: float) -> dict`
  - Compress old low-score memories into summary entries.

Groups similar entries by keyword overlap, creates a compressed
summary per group, removes originals, rewrites JSONL + edges.
Returns stats dict.

### `core/memory_curator.py`

- `classify(content: str) -> tuple[str, float]`
  - Classify content and return (classification, confidence 0-1).

Uses multiple matching rules; highest confidence wins.
- `value_score(cls: str) -> float`
  - Return decay-resistant priority score based on classification.

Score range: 0.0 (forget immediately) to 1.0 (permanent).
Used as base for entry.score in MemoryStore.
- `detect_conflicts(content: str, existing_entries: list[dict]) -> Optional[dict]`
  - Check if new content conflicts with or updates existing memories.

Returns:
    {
        "type": "update" | "duplicate" | "contradiction" | None,
        "target_id": str,
        "reason": str,
    
- `_detect_contradiction(a: str, b: str) -> bool`
  - Rough contradiction detection via negation markers.
- `curate(content: str) -> dict`
  - Curate a memory write attempt. Returns a decision dict.

Args:
    content: The text to be remembered.
    tags: Proposed tags.
    source: Who created this memory.
    existing_entries: Recent memory

### `core/messaging/__init__.py`

- `register(name: str, description: str, config_key: str)`
  - Decorator to register a connector class.
- `get_connector(name: str) -> Optional[ConnectorDef]`
  - Get a connector definition by name.
- `BaseConnector.__init__(self, config: dict, on_message: Callable[[Message], None])`
- `BaseConnector.connect(self) -> bool`
  - Connect to the platform. Return True on success.
- `BaseConnector._is_chat_busy(self, chat_id: str) -> bool`
  - Check if this chat already has an active task.
- `BaseConnector._mark_chat_busy(self, chat_id: str)`
  - Mark a chat as having an active task.
- `BaseConnector._unmark_chat_busy(self, chat_id: str)`
  - Unmark a chat when its task completes.
- `BaseConnector._acquire_slot(self) -> bool`
  - Try to acquire a processing slot. Returns True if slot available.
- `BaseConnector._release_slot(self, chat_id: str)`
  - Release a processing slot. If messages are queued, process the next one.
- `BaseConnector._enqueue_message(self, chat_id: str, user_id: int, user_name: str, text: str, msg: dict, msg_type: str) -> int`
  - Enqueue a message for later processing. Returns queue position (1-indexed).

msg_type: 'text', 'photo', 'document', or 'voice' — determines which handler is called.
- `BaseConnector._dequeue_next(self)`
  - Try to dequeue and process the next message. Called on slot release.
- `BaseConnector._dispatch_queued(self, item: dict)`
  - Dispatch a queued message to the appropriate handler based on msg_type.
- `BaseConnector._record_batch_result(self, chat_id: str, summary: str, msg_type: str)`
  - Record a concurrent task's result for batch synthesis.
- `BaseConnector._synthesize_batch(self)`
  - Consolidate all batch results and write consolidated memory for evolution.
- `BaseConnector.disconnect(self)`
  - Disconnect from the platform.
- `BaseConnector.send(self, chat_id: str, text: str) -> bool`
  - Send a message to a chat. Return True on success.
- `BaseConnector.start(self)`
  - Start the polling loop in a background thread.
- `BaseConnector.stop(self)`
  - Stop the polling loop.
- `BaseConnector._poll_loop(self)`
  - Platform-specific polling loop.
- `BaseConnector._load_session_index(self)`
  - Load all saved session file IDs into memory (not full history).
- `BaseConnector._get_or_create_session(self, chat_id: str, first_message: str) -> dict`
  - Get (or create) the active in-memory session for this chat.
If creating new, auto-name from first_message (first 40 chars, stripped).
- `BaseConnector._save_session_to_disk(self, session: dict)`
  - Write session to disk as JSON.
- `BaseConnector._list_saved_sessions(self) -> str`
  - Return formatted list of all saved sessions.
- `BaseConnector._load_session_from_disk(self, session_id: str) -> Optional[dict]`
  - Load a full session from disk. Returns None if not found.
- `BaseConnector._delete_session(self, session_id: str) -> bool`
  - Delete a saved session from disk. Returns True if deleted.
- `BaseConnector._save_restart_chat_id(self, chat_id: str)`
  - Save chat_id to .restart_pending so the new process can notify.
- `BaseConnector.route(self, msg: Message) -> Optional[str]`
  - Route a message to BAW and return the response.

Handles built-in commands before passing to BAW.
- `BaseConnector._dispatch_task(self, text: str, chat_id: str | None, _depth: int) -> str`
  - Route a task: multi-task split → direct shortcuts → chat bypass → agent loop. Recursive.
- `BaseConnector._chat_response(self, text: str, chat_id: str | None) -> str`
  - Direct lightweight LLM response. No tools, no court, no agent loop.
Loads session history + queries memory for context.
- `BaseConnector._execute_multi_task(self, text: str, chat_id: str | None, _depth: int) -> str`
  - Detect numbered tasks, execute each via _dispatch_task (recursive).

Each sub-task result is sent as its own permanent Telegram message.
Only a compact summary is returned (edited into the placeholder
- `BaseConnector._handle_task_command(self, chat_id: str, action: str, arg: str) -> str`
- `BaseConnector._summarize_session(self, chat_id: str) -> str`
  - Summarize the current session via LLM.
- `BaseConnector._compress_session(self, config: dict, data_dir: Path, session: dict) -> str`
  - Compress session: use context.py compact summary instead of LLM summarization.
Keeps last 4 messages + summary header. Saves summary to memory.
- `BaseConnector._pickup_last_session(self, chat_id: str) -> str`
  - Find the most recent interrupted session and resume it.
- `BaseConnector._MODELS(self) -> list`
  - Derive model list from config, with fallback.
- `BaseConnector._baw_ensure(self)`
  - Lazy-import BAW modules in-process (no subprocess).
- `BaseConnector._baw_reload(self) -> str`
  - Hot-reload tools, config, and SOUL without restarting the bot.
- `BaseConnector._baw_cfg_set(self, key: str, value: str) -> str`
  - Set a config value both in-file and in-memory cache.
- `BaseConnector._format_tool_log(messages: list[dict]) -> str`
  - Compact step log — one line per tool call, no raw args.
- `BaseConnector._update_with_progress(self, chat_id: str) -> str`
  - Standardized update flow with per-step progress display.
- `BaseConnector._run_baw(self, prompt: str, chat_id: str | None) -> str`
  - Run BAW in-process (no subprocess), with session history.
- `BaseConnector._run_model_council(self, goal: str) -> str`
  - Query ALL available providers in parallel for strategic advice.

Each provider's first chat-capable model is queried with the goal.
Returns a formatted council report.
- `BaseConnector._handle_focus(self, goal: str, chat_id: str | None) -> str`
  - Focus Mode: Model Council → Synthesis → Relentless Execution.

1. Query ALL available providers for strategic advice (parallel)
2. Synthesize the best approach
3. Execute relentlessly until goal achie

### `core/messaging/discord.py`

- `DiscordConnector.__init__(self, config: dict, on_message)`
- `DiscordConnector.connect(self) -> bool`
- `DiscordConnector.disconnect(self)`
- `DiscordConnector.send(self, chat_id: str, text: str) -> bool`
  - Send a message to a Discord channel.
- `DiscordConnector._poll_loop(self)`
  - Poll for new messages in allowed channels.

Uses a simple polling approach: periodically fetch recent messages
from each configured channel and process new ones.
- `DiscordConnector._discover_channels(self)`
  - Auto-discover accessible channels.
- `DiscordConnector._poll_channel(self, channel_id: str)`
  - Fetch recent messages from a channel and process new ones.
- `DiscordConnector._handle_message(self, channel_id: str, msg: dict)`
  - Process a single Discord message.

### `core/messaging/matrix.py`

- `MatrixConnector.__init__(self, config: dict, on_message)`
- `MatrixConnector.connect(self) -> bool`
- `MatrixConnector.disconnect(self)`
- `MatrixConnector.send(self, chat_id: str, text: str) -> bool`
  - Send a message to a Matrix room.
- `MatrixConnector._poll_loop(self)`
  - Long-poll Matrix sync for new messages.
- `MatrixConnector._handle_event(self, room_id: str, event: dict)`
  - Process a Matrix room event.

### `core/messaging/signal.py`

- `SignalConnector.__init__(self, config: dict, on_message)`
- `SignalConnector.connect(self) -> bool`
- `SignalConnector.disconnect(self)`
- `SignalConnector.send(self, chat_id: str, text: str) -> bool`
  - Send a Signal message via signal-cli.
- `SignalConnector._poll_loop(self)`
  - Signal uses signal-cli daemon mode for receiving.

See: https://github.com/AsamK/signal-cli/wiki/DBus-service
Or use the JSON-RPC mode for message reception.

This connector requires manual daemon set

### `core/messaging/slack.py`

- `SlackConnector.__init__(self, config: dict, on_message)`
- `SlackConnector.connect(self) -> bool`
- `SlackConnector.disconnect(self)`
- `SlackConnector.send(self, chat_id: str, text: str) -> bool`
  - Send a message to a Slack channel/user.
- `SlackConnector._split_text(self, text: str) -> list[str]`
  - Split text into Slack-safe chunks.
- `SlackConnector._start_socket_mode(self)`
  - Open Socket Mode WebSocket connection.
- `SlackConnector._on_ws_open(self, ws)`
- `SlackConnector._on_ws_message(self, ws, message)`
- `SlackConnector._on_ws_error(self, ws, error)`
- `SlackConnector._on_ws_close(self, ws, close_status_code, close_msg)`
- `SlackConnector._handle_event(self, event: dict)`
  - Process a Slack event.
- `SlackConnector._poll_loop(self)`
  - Socket Mode uses WebSocket — no polling needed.

This method keeps the connector alive for lifecycle management.

### `core/messaging/telegram.py`

- `TelegramConnector.__init__(self, config: dict, on_message)`
- `TelegramConnector.connect(self) -> bool`
  - Test connection by fetching bot info.
- `TelegramConnector._notify_restart(self)`
  - Send 'Back Online' notification if this was a restart.
- `TelegramConnector._register_commands(self)`
  - Register bot command menu via setMyCommands.
- `TelegramConnector.disconnect(self)`
- `TelegramConnector.send(self, chat_id: str, text: str, edit_msg_id: str) -> str`
  - Send or edit a message. Returns message_id if successful, empty string if failed.
If edit_msg_id is provided, edits that message instead of sending new one.

Long messages (>2000 chars) are auto-trunc
- `TelegramConnector._resolve_media_path(self, fpath: str) -> str`
  - Resolve a MEDIA file path. Verifies the file actually exists
and is readable. If not, logs and returns empty string so the
caller can skip the attachment (instead of sending 'File not found'
error to 
- `TelegramConnector._send_text(self, chat_id: str, text: str) -> str`
  - Send a plain text message. Returns message_id string or empty on failure.
- `TelegramConnector._edit_text(self, chat_id: str, message_id: str, text: str) -> str`
  - Edit an existing message. Returns message_id on success, empty on failure.
- `TelegramConnector.send_typing(self, chat_id: str) -> bool`
  - Send typing indicator to show '...' in Telegram.
- `TelegramConnector._send_media(self, chat_id: str, file_path: str)`
  - Send a media file using the appropriate Telegram API endpoint.
- `TelegramConnector._send_media_as_document(self, chat_id: str, fpath)`
  - Fallback: send any file as a document.
- `TelegramConnector._download_file(self, file_id: str, file_name: str) -> str`
  - Download a file from Telegram Bot API. Returns local path.
- `TelegramConnector._extract_file_content(self, file_path: str) -> str`
  - Extract text content from a file based on extension. Returns str.
- `TelegramConnector._handle_media_msg(self, chat_id: str, user_id, user_name: str, msg: dict, msg_type: str)`
  - Single non-text message: acquire slot or queue (never drop).
- `TelegramConnector._buffer_media_group(self, chat_id: str, group_id: str, msg: dict)`
  - Buffer a media group message. Start/reset a timer; when it fires, process all.
- `TelegramConnector._process_media_group(self, chat_id: str, group_id: str)`
  - Timer fired — all media in this group should have arrived. Queue them all.
- `TelegramConnector._process_document_file(self, chat_id: str, doc: dict, msg: dict)`
  - Download, extract, and analyze a document via BAW. Inline edit — one message.
- `TelegramConnector._process_image_file(self, chat_id: str, photo_data: dict, msg: dict)`
  - Download an image and analyze with vision AI (MiniMax API → Stepfun → OCR). Inline edit.
- `TelegramConnector._process_voice_file(self, chat_id: str, voice_data: dict, msg: dict)`
  - Download audio, check STT capability, then transcribe or present options.
- `TelegramConnector._scan_all_models(config: dict) -> list[dict]`
  - Scan all providers/models from config and return flat list with capability info.
- `TelegramConnector._send_as_tts(self, chat_id: str, text: str)`
  - Convert response text to speech and send as audio message.
- `TelegramConnector._poll_loop(self)`
  - Long-polling loop.
- `TelegramConnector._handle_update(self, update: dict)`
  - Process a single Telegram update (non-blocking, concurrent).
- `TelegramConnector._set_reaction(self, chat_id: int, message_id: int, emoji: str) -> bool`
  - Set a reaction emoji on a telegram message. Falls back to big emoji if unsupported.
- `TelegramConnector._process_btw(self, chat_id: str, question: str)`
  - Quick LLM answer — no agent loop, no court, no queue. Runs in own thread.
- `TelegramConnector._process_message(self, chat_id, user_id, user_name, text, msg)`
  - Process a message in background thread (runs route() + send()).
- `TelegramConnector._get_providers_config(self) -> dict`
  - Get provider → models mapping from config + auto-discovered models.
- `TelegramConnector._discover_provider_models(self, providers: dict) -> dict`
  - Try to auto-discover available models from each provider's API.
- `TelegramConnector._get_current_model_for_role(self, role: str) -> str | None`
  - Read current model for a role from config. Returns None if unset (inherits default).
- `TelegramConnector._send_role_selector(self, chat_id: str, text: str) -> bool`
  - Show role selection keyboard with current settings.
- `TelegramConnector._send_role_buttons(self, chat_id: str, msg_id: int)`
  - Edit existing message to show role selection buttons with current settings.
- `TelegramConnector._send_model_selector_text(self, chat_id: str, text: str) -> bool`
  - Parse [MODEL_SELECT] formatted text and send provider-level keyboard.
- `TelegramConnector._send_model_buttons(self, chat_id: str, msg_id: int, provider: str, current_model: str)`
  - Edit message to show models for a specific provider.
- `TelegramConnector._handle_callback(self, cb: dict)`
  - Handle inline keyboard callback (role → provider → model selection).

### `core/messaging/whatsapp.py`

- `WhatsAppConnector.__init__(self, config: dict, on_message)`
- `WhatsAppConnector.connect(self) -> bool`
- `WhatsAppConnector.disconnect(self)`
- `WhatsAppConnector.send(self, chat_id: str, text: str) -> bool`
  - Send via WhatsApp Cloud API.
- `WhatsAppConnector._poll_loop(self)`
  - WhatsApp uses webhooks, not polling.

Set up a webhook server that listens for incoming messages
and routes them through BAW. See docs/webhooks.md for setup.

### `core/model_discovery.py`

- `_find_env_key(env_var: str) -> bool`
  - Check if an API key exists in .env or environment.
- `guess_provider(model_id: str) -> Optional[str]`
  - Guess which provider a model belongs to.

Returns provider name (e.g. 'anthropic') or None.
- `auto_discover_model(config: dict, model_id: str, data_dir: Path | None) -> Optional[dict]`
  - Try to auto-discover and add a model to config.

Priority:
  1. Already-configured providers (have API key in .env) — search their known model patterns
  2. Free/community alternatives (Agnes AI) — on
- `_guess_capabilities(model_id: str) -> list[str]`
  - Guess capabilities from model ID patterns.

Used by auto_discover_all_models to tag models discovered via /v1/models.
- `auto_discover_all_models(config: dict) -> int`
  - Query /v1/models for every configured provider and add any missing models.

Returns number of new models added.
- `_get_configured_providers(config: dict) -> set[str]`
  - Return set of provider names that have API keys configured in .env.
- `_add_model_to_provider(config: dict, provider_name: str, model_id: str, pinfo: dict, data_dir: Path | None) -> dict`
  - Add a model to a provider and save config. Returns model info dict.
- `_guess_context_window(model_id: str) -> int`
  - Guess context window from model name.
- `_save_config(config: dict, data_dir: Path | None)`
  - Save config back to disk. Handles read-only 444 lock by temporarily chmod 644.

### `core/monitor.py`

- `track_error(provider: str, model: str, error_type: str, detail: str)`
  - Record an error for monitoring.
- `get_error_rate(hours: int) -> dict`
  - Get error rate per provider in the last N hours.
- `get_health_score_history(days: int) -> list[dict]`
  - Get health score history.
- `record_health_score(score: float)`
  - Record a health score for history.
- `alert_if_high_error_rate(threshold: int, hours: int) -> str | None`
  - Check recent error rate and return alert message if too high.
Returns None if rate is normal, or an alert string if elevated.

### `core/night_court.py`

- `format_nightly_summary(now: Optional[float]) -> str`
  - Return the previous-24-hour court activity summary as a string.

Args:
    now: epoch seconds for "now". Defaults to time.time(). Exposed
         for testing so the test can pin a fixed time.

Return

### `core/output_validator.py`

- `validate_output(output: str) -> str`
  - Run all quality gates. Returns cleaned output.

Args:
    output: Raw LLM/agent output to validate
    prompt: Original user prompt (for hallucination context)

Returns:
    Cleaned output string
- `_strip_html(text: str) -> str`
  - Strip dangerous HTML tags, preserve safe formatting tags.

Telegram supports parse_mode=HTML with these safe tags:
<b>, <i>, <u>, <s>, <code>, <pre>, <a>
All other tags (script, style, div, etc.) are 
- `_compress_blank_lines(text: str) -> str`
- `_markdown_bold_to_html(text: str) -> str`
  - Convert **text** or __text__ (Markdown bold) to <b>text</b> (HTML bold).

Handles:
- **bold text** → <b>bold text</b>
- __bold text__ → <b>bold text</b>
- Nested punctuation inside bold: **1. Item** →
- `_redact_credentials(text: str) -> str`
- `_strip_summary_sections(text: str) -> str`
- `_check_hallucination(text: str, prompt: str) -> str`
- `_strip_stale_lines(text: str) -> str`
- `_enforce_length(text: str) -> str`
- `_balance_html(text: str) -> str`
  - Ensure all inline HTML tags are properly closed.

Detects unclosed opening tags and appends closing tags at the end.
Does NOT fix deeply nested cross-contamination — just prevents
Telegram's HTML pars
- `_compress_verbose(text: str) -> str`
  - Aggressively compress output that has too many sections or is too long.

Detects:
- >3 bold/markdown section headers → collapse to compact format
- [PLAN] Diagnosis sections at end → strip
- Token foo
- `score_output(output: str) -> dict`
  - Score output quality for self-diagnosis. Returns dict with scores.

### `core/paths.py`

- `ensure_data_file(name: str) -> Path`
  - Return a path inside data/ for a given filename, creating parent dirs.

### `core/permission.py`

- `PermissionEngine.__init__(self, config: dict)`
- `PermissionEngine._match_rule(self, rule: dict, tool_name: str, params: dict) -> bool`
  - Check if a permission rule matches a tool call.
- `PermissionEngine.check(self, tool_name: str, params: dict) -> dict`
  - Check permission for a tool call.
Returns: {"decision": "allow"|"prompt"|"deny", "reason": str}
- `PermissionEngine.session_allow(self, tool_name: str, params: dict)`
  - Remember a one-time or session-level approval.
- `PermissionEngine.session_deny(self, tool_name: str, params: dict)`
  - Remember a one-time or session-level denial.

### `core/permissions.py`

- `_perm_path(data_dir: Optional[Path]) -> Path`
- `_load(data_dir: Optional[Path]) -> dict`
- `_save(perms: dict, data_dir: Optional[Path])`
- `get_level(scope: str, data_dir: Optional[Path]) -> str`
  - Return the effective permission level for scope.
Priority: session > permanent config > default (ask).
- `check(scope: str, data_dir: Optional[Path]) -> str`
  - Check permission. Returns 'granted', 'blocked', or 'ask'.
- `grant(scope: str, duration: str, data_dir: Optional[Path])`
  - Grant permission for a scope.
duration: 'session' (this restart), 'permanent' (persisted), or '5m'/'1h'/'1d'.
- `block(scope: str, duration: str, data_dir: Optional[Path])`
  - Block a scope.
- `reset(scope: str, data_dir: Optional[Path])`
  - Reset permission(s) to default (ask). scope=None = reset all.
- `list_perms(data_dir: Optional[Path]) -> dict`
  - List all current permissions (both session and persistent).
- `_parse_duration(s: str) -> int`

### `core/profile.py`

- `_profiles_dir(data_dir: Path) -> Path`
- `_active_symlink(data_dir: Path) -> Path`
- `cmd_profile_list(data_dir: Path)`
  - List all profiles.
- `cmd_profile_create(data_dir: Path, name: str)`
  - Create a new profile.
- `cmd_profile_use(data_dir: Path, name: str)`
  - Switch to a profile. Creates config/env symlinks.
- `cmd_profile_delete(data_dir: Path, name: str)`
  - Delete a profile.

### `core/render.py`

- `bold(text: str) -> str`
- `italic(text: str) -> str`
- `code(text: str) -> str`
- `pre(text: str, lang: str) -> str`
- `link(url: str, text: str) -> str`
- `blockquote(text: str) -> str`
- `spoiler(text: str) -> str`
- `_e(text: str) -> str`
  - Escape HTML entities (only &, <, >).
- `render_adversarial_court(devil_score: float, devil_content: str, angel_score: float, angel_content: str, decision: str, should_stop: bool) -> str`
  - Render the Angel/Devil court output as HTML.
- `render_cost(calls: list[dict], total: float) -> str`
  - Render cost summary as HTML.
- `render_fact_check(action: str, claims: list, message: str, verbose: bool) -> str`
  - Render fact checker output as HTML.
- `render_strategy_report(strategies: list[str], attempts: int, final_decision: str) -> str`
  - Render a strategy recovery report as HTML.
- `wrap_html(body: str) -> str`
  - Wrap content in a full HTML document structure.

### `core/router.py`

- `score_complexity(prompt: str, estimated_tool_count: int, context_tokens: int) -> int`
  - Return complexity score 0-10.

Args:
    prompt: user's raw prompt
    estimated_tool_count: hint from prior call history (0 if unknown)
    context_tokens: estimated context size for this turn

Retur
- `tier_of(score: int) -> str`
- `get_tier_preferences(config: dict) -> dict[str, list[str]]`
  - Get tier → model preference list.

Precedence (highest first):
1. config['router']['tier_preferences'][<tier>]   ← user override
2. config['model']['default']                      ← user's primary mod
- `pick_model_for_tier(tier: str, config: dict) -> str`
  - Pick a model for the tier.

Two strategies (config.router.strategy):
  - "simple" (default): first available model in preference list
  - "balanced": weighted round-robin through available models

The
- `_get_available_chat_models(config: dict) -> set[str]`
  - Return set of model IDs that have chat capability and API keys.
- `should_delegate(score: int) -> bool`
  - Return True if this task should go to a sub-agent.

Graduated delegation:
  - 0-5:  INLINE direct — model handles everything inline
  - 6-7:  INLINE with sub-agent hint — model CAN delegate sub-tasks

- `get_inline_mode(score: int) -> str | None`
  - Return the inline execution mode for a given score.

Returns:
  INLINE_DIRECT    -> 0-5: strict inline, no sub-agent
  INLINE_WITH_HINT -> 6-7: inline but model may delegate sub-tasks
  None          
- `needs_multi_tier(score: int) -> bool`
  - Return True if this task should use multi-tier cascade.

Multi-tier means: delegate to sub-agent → if result indicates
more decomposition needed, re-delegate that sub-task to another
sub-agent (or the
- `route_task(prompt: str, config: dict, estimated_tool_count: int, context_tokens: int) -> RouteDecision`
  - Make a routing decision for a task.

Returns a RouteDecision with: score, tier, model, delegate?, multi_tier?
- `should_re_delegate(sub_result: str) -> bool`
  - Inspect a sub-agent's result. If it indicates the task needs
further decomposition (e.g. returns a Plan with sub-steps, or
says "需要再分" / "need to split"), return True so the orchestrator
re-delegates 
- `RouteDecision.__init__(self, score: int, tier: str, model_id: str, delegate: bool, multi_tier: bool, inline_mode: str | None, reasoning: str)`
- `RouteDecision.__repr__(self)`

### `core/scheduler.py`

- `ScheduledTask.__init__(self, name: str, cron: str, command: str, skill: str, skill_args: str, enabled: bool, description: str)`
- `ScheduledTask.from_dict(cls, d: dict) -> 'ScheduledTask'`
- `ScheduledTask.to_dict(self) -> dict`
- `ScheduledTask.next_run(self, base: datetime | None) -> datetime | None`
  - Calculate next run time from a base time (default: now).
- `ScheduledTask.should_run(self, base: datetime, last_run: datetime | None) -> bool`
  - Check if this task should fire now.
- `Scheduler.__init__(self, data_dir: Path | str)`
- `Scheduler._schedule_path(self) -> Path`
- `Scheduler._state_path(self) -> Path`
- `Scheduler._load(self)`
  - Load tasks from YAML and state from JSON.
- `Scheduler._save_state(self)`
- `Scheduler.save_schedule(self)`
  - Write tasks back to YAML.
- `Scheduler.list_tasks(self) -> list[ScheduledTask]`
- `Scheduler.add_task(self, task: ScheduledTask)`
- `Scheduler.remove_task(self, name: str) -> bool`
- `Scheduler.toggle_task(self, name: str, enabled: bool | None) -> bool`
- `Scheduler._check_cron_jobs(self, now: datetime) -> list[dict]`
  - Also check user-created cron jobs from ~/.baw/cron/jobs.json.
- `Scheduler._cron_tz(self) -> str`
  - Get configured cron timezone from config.yaml.
- `Scheduler._cron_next(self, sched: str, after: float) -> float`
  - Calculate next run timestamp from cronjob schedule format.
- `Scheduler._cron_next_tz(self, now_dt: datetime, h: int, m: int, for_weekly: int | None) -> float`
  - Calculate next run respecting config timezone, return UTC timestamp.
- `Scheduler.poll(self) -> list[dict]`
  - Check all tasks, fire any that are due. Returns list of fired tasks.
- `Scheduler._execute(self, task: ScheduledTask, now: datetime) -> str`
  - Delegate a scheduled task to the background executor.

Supports two modes:
  - Normal: runs through BAW agent loop (default)
  - Shell:  if command starts with '!', runs as raw shell command
- `Scheduler.cron_status_report(self) -> str`
  - Scan recent task directories and report failures.

Returns a compact summary of cron task health.
- `Scheduler.start(self)`
  - Start the polling daemon in a background thread.
- `Scheduler.stop(self)`
- `Scheduler._loop(self)`
- `Scheduler.status_report(self) -> str`
  - Generate a text summary of all scheduled tasks.

### `core/search.py`

- `register_search_provider(name: str, module: object)`
  - Register a search provider module.

The module must export:
  NAME: str
  DESCRIPTION: str
  SETUP_GUIDE: str (markdown)
  API_REFERENCE: str (markdown)
  search(query, limit=5, **kwargs) -> list[Sear
- `get_provider(name: str) -> Optional[object]`
  - Get a registered provider module by name.
- `search(query: str, provider: str, limit: int) -> list[dict]`
  - Search using the specified provider.

Args:
    query: Search query string
    provider: Provider name (default: duckduckgo)
    limit: Max results
    **kwargs: Provider-specific parameters

Returns:
- `get_setup_guide(name: str) -> str`
  - Get the setup guide for a provider.
- `get_api_reference(name: str) -> str`
  - Get the API reference for a provider.
- `SearchResult.__init__(self, title: str, url: str, snippet: str, content: str, source: str)`
- `SearchResult.to_dict(self) -> dict`
- `SearchResult.__repr__(self)`

### `core/setup.py`

- `load_config(data_dir: Path) -> dict`
- `save_config(data_dir: Path, config: dict)`
  - Write config dict to config.yaml with managed key protection.
- `_print_header(title: str)`
- `_print_section(title: str)`
- `_print_item(key: str, value: str, indent: int)`
- `_print_note(text: str)`
- `_input(prompt: str, default: str) -> str`
  - Prompt with color and optional default.
- `_ok(text: str)`
- `_warn(text: str)`
- `_confirm(prompt: str, default: bool) -> bool`
- `cmd_config_list(data_dir: Path)`
- `cmd_config_get(data_dir: Path, key: str)`
- `cmd_config_set(data_dir: Path, key: str, value: str)`
- `_validate_api_key(provider: str, base_url: str, api_key: str, model: str) -> tuple[bool, str]`
  - Send a test request to validate an API key. Returns (ok, message).
- `_explain_plan(provider: str) -> str`
- `_collect_models_by_capability(providers: dict, capability: str) -> list[tuple[str, str, str]]`
  - Collect model IDs from providers filtered by capability.

Returns list of (model_id, provider_key, label).
- `_pick_model_menu(providers: dict, prompt: str, exclude_provider: str | None, capability: str, current_model: str | None, extra_models: list[str] | None) -> str`
  - Show a numbered menu of models from configured providers, return chosen model ID.
- `cmd_setup(data_dir: Path)`
- `cmd_config_edit(data_dir: Path)`
  - Open config.yaml in editor.
- `cmd_config_path(data_dir: Path)`
  - Print config.yaml path.
- `cmd_config_env_path(data_dir: Path)`
  - Print .env path.
- `cmd_config_check(data_dir: Path)`
  - Validate config for required sections.

### `core/skills.py`

- `Skill.__init__(self, name: str, description: str, steps: list[dict], tools: list[str], config: dict, error_handling: dict)`
- `Skill.from_yaml(cls, path: Path) -> 'Skill'`
- `Skill.to_dict(self) -> dict`
- `Skill.build_prompt(self, step_index: int, user_args: str) -> str`
  - Build the execution prompt for a step or the whole skill.
- `Skill.get_mode(self) -> str`
- `Skill.get_max_retries(self) -> int`
- `Skill.get_on_failure(self) -> str`
- `SkillRegistry.__init__(self, data_dir: Path | str)`
- `SkillRegistry._skills_path(self) -> Path`
- `SkillRegistry._load(self)`
- `SkillRegistry._create_sample_skills(self, sp: Path)`
  - Create example skills for the user.
- `SkillRegistry.list_skills(self) -> list[Skill]`
- `SkillRegistry.get_skill(self, name: str) -> Skill | None`
- `SkillRegistry.reload(self)`
- `SkillRegistry.status_report(self) -> str`

### `core/task_manager.py`

- `AsyncTask.__init__(self, task_id: str, prompt: str, task_dir: Path)`
- `AsyncTask.status(self) -> str`
- `AsyncTask.status(self, val: str)`
- `AsyncTask.output(self) -> str`
- `AsyncTask.error(self) -> str`
- `AsyncTask.cancel(self)`
  - Cancel the task by sending SIGTERM and marking status.
- `AsyncTask.to_dict(self) -> dict`
- `TaskManager.__new__(cls)`
- `TaskManager.__init__(self, data_dir: Path | str | None)`
- `TaskManager._load_existing(self)`
  - Scan tasks directory for existing tasks.
- `TaskManager._pid_alive(self, task: AsyncTask) -> bool`
  - Check if the task's PID is still alive.
- `TaskManager._find_baw_command(self) -> str`
  - Find the BAW CLI command to use in subprocesses.

Priority:
1. ~/.local/bin/baw (the bash wrapper — most robust)
2. sys.argv[0] (current script path)
3. 'baw' (fallback, assumes it's in PATH)
- `TaskManager.submit(self, prompt: str, mode: str) -> AsyncTask`
  - Submit a new background task. Subprocess starts immediately.

The child process is DETACHED from parent (start_new_session=True),
so 'baw --delegate' can exit while the task runs in background.
- `TaskManager._start_cleaner(self)`
  - Start background cleaner thread (monitoring only, not execution).
- `TaskManager._cleaner_loop(self)`
  - Check running tasks and update stale statuses.
- `TaskManager._check_stale(self)`
  - Check if any running tasks have completed/failed.
- `TaskManager.cancel(self, task_id: str) -> bool`
  - Cancel a task by ID.
- `TaskManager.get(self, task_id: str) -> AsyncTask | None`
- `TaskManager.list_tasks(self, status_filter: str | None) -> list[AsyncTask]`
- `TaskManager.summary(self) -> str`
  - Human-readable summary of all tasks.

### `core/test_runner.py`

- `dispatch_test_command(subcmd: str) -> str`
  - Dispatch a /test sub-command.

### `core/todo_state.py`

- `get_state(session_id: Optional[str], data_dir: Optional[Path]) -> TodoState`
- `TodoItem.__post_init__(self)`
- `TodoItem.to_dict(self) -> dict`
- `TodoState.__init__(self, data_dir: Path | str | None, session_id: str)`
- `TodoState._load(self)`
- `TodoState._save(self)`
- `TodoState._touch(self, it: TodoItem)`
- `TodoState._append_followup_log(self, it: TodoItem)`
  - Mirror follow-ups to an append-only cross-session log.
- `TodoState.add(self, content: str, type: ItemType, note: str, parent_id: str) -> TodoItem`
- `TodoState.update(self, item_id: str) -> Optional[TodoItem]`
- `TodoState.complete(self, item_id: str) -> Optional[TodoItem]`
- `TodoState.cancel(self, item_id: str) -> Optional[TodoItem]`
- `TodoState.start(self, item_id: str) -> Optional[TodoItem]`
- `TodoState.remove(self, item_id: str) -> bool`
- `TodoState.get(self, item_id: str) -> Optional[TodoItem]`
- `TodoState.list(self, active_only: bool, type: Optional[ItemType]) -> list[TodoItem]`
- `TodoState.stats(self) -> dict`
- `TodoState.load_pending_followups(self) -> list[TodoItem]`
  - Read all followups still marked pending across all session files.

Returns TodoItem instances (not bound to self._items). Useful at boot
to surface work carried over from a previous session.

### `core/token_killer.py`

- `_filter_ansi(text: str) -> str`
  - Strip ANSI escape codes from text.
- `_collapse_empty_lines(text: str) -> str`
  - Replace 3+ consecutive blank lines with 2.
- `_deduplicate_lines(text: str) -> str`
  - Collapse 3+ consecutive identical lines into 'line [xN]'.
- `_truncate_smart(text: str, max_chars: int) -> str`
  - Truncate text smartly: keep head (70%) + tail (30%) with skip marker.
- `_extract_failures(text: str, command: str) -> str`
  - For test commands, extract failure lines only.
- `_compress_terminal(name: str, output: str, args: dict) -> str`
  - Compress terminal/bash tool output.
- `_compress_git_diff(output: str) -> str`
  - Compress git diff: keep file headers + changed line counts.
- `_compress_git_status(output: str) -> str`
  - Compress git status: group by status type.
- `_compress_git_log(output: str) -> str`
  - Compress git log: one line per commit.
- `_compress_search_files(output: str, args: dict) -> str`
  - Compress search_files output: group by file, limit per-file matches.
- `_compress_web_search(output: str, args: dict) -> str`
  - Compress web search results: keep title + first 200 chars.
- `_is_schematic_content(text: str) -> bool`
  - Check if text contains circuit/schematic content that needs higher fidelity.
- `compress_tool_output(name: str, raw_output: str, args: dict | None) -> str`
  - Compress tool output before it enters LLM context.

Args:
    name: Tool name (e.g., 'terminal', 'read_file', 'search_files')
    raw_output: Raw tool output string
    args: Tool arguments dict (for 
- `should_activate_court(prompt: str) -> bool`
  - Determine if the adversarial court (Devil + Angel) should run.

Court is activated when:
1. System-modifying actions (write, delete, deploy, config change)
2. Irreversible or dangerous operations
3. A
- `estimate_task_complexity(prompt: str) -> str`
  - Estimate task complexity to skip unnecessary overhead.

Returns: 'simple', 'moderate', or 'complex'

### `core/tone.py`

- `detect_tone_switch(user_message: str) -> Optional[str]`
  - Detect if user wants to switch tone. Returns new tone name or None.
- `format_tone_confirmation(old_tone: str, new_tone: str) -> str`
  - Format a confirmation message for tone switch.

### `core/tool_policy.py`

- `classify_error(exe_result: str) -> str`
  - Classify a tool error string into a category.

Returns one of:
  - ``"permanent"``  — will never resolve on retry
  - ``"transient"``  — may resolve on retry
  - ``"tool_error"`` — generic tool output
- `get_max_retries(tool_name: str) -> int`
  - Return the maximum allowed retry count for a given tool name.

This is the number of consecutive failures BAW may experience before
the loop stops the tool path. ``0`` means stop on first failure.

### `core/tool_schema.py`

- `validate_tool_def(tool_def: Dict[str, Any]) -> List[str]`
  - Validate a TOOL_DEF dict. Returns a list of warnings.

Raises ``ToolSchemaError`` on hard failures (missing required keys,
wrong types, invalid risk_level). Soft warnings (extra keys,
description too 
- `validate_tool_def_file(path: Path) -> Tuple[bool, List[str]]`
  - Static-validate a tool file by parsing its TOOL_DEF AST.

Useful for CI / pre-commit. Does NOT import the module — just checks
the dict literal shape so we can audit without side effects.

### `core/tools.py`

- `register(name: str, description: str, handler: Callable, parameters: dict, risk_level: str)`
  - Register a tool with BAW's tool registry.

Validates the schema (see ``core.tool_schema``) before storing.
Hard failures raise ``ToolSchemaError`` so the bug surfaces at
registration time, not silentl
- `get_tool(name: str) -> Optional[ToolDef]`
  - Get a tool by name.
- `execute_tool(name: str, arguments: dict, timeout: int) -> str`
  - Execute a tool by name with validated arguments + timeout guard + auto-retry.

### `core/tribunal.py`

- `_get_tribunal_config(cfg: dict | None) -> dict`
  - Extract tribunal section from config.
- `_get_default_bench(cfg: dict | None) -> list[dict]`
  - Default bench: use first 2 available models from config.
- `_get_default_chief(cfg: dict | None) -> dict`
  - Default chief: the highest-tier model available.
- `_call_model(provider: str, model: str, messages: list[dict], temperature: float, timeout: float) -> dict`
- `_invoke_judge(judge_cfg: dict, prompt: str, system: str) -> JudgeVerdict`
- `_score_consensus(verdicts: list[JudgeVerdict]) -> float`
- `_build_synthesis_prompt(original: str, verdicts: list[JudgeVerdict], consensus: float) -> str`
- `tribunal_command(query: str) -> str`
  - Entry point for Telegram /tribunal command.
- `court_tribunal_ruling(case_text: str, context: str) -> TribunalRuling`
  - Called by BAW Court when a case is escalated to Tier-2 (disputed/complex).
- `Tribunal.__init__(self, bench: list[dict] | None, chief: dict | None, max_workers: int)`
- `Tribunal.deliberate(self, prompt: str, system: str, temperature: float) -> TribunalRuling`

### `core/tts.py`

- `minimax_tts(text: str, api_key: str, voice: str, model: str, language: str) -> Optional[bytes]`
  - Call MiniMax T2A API to convert text to speech.

Returns raw audio bytes (MP3), or None on failure.
- `save_audio_bytes(data: bytes, name: str) -> str`
  - Save audio bytes to temp file and return path.

### `core/update.py`

- `_run(cmd: list[str], timeout: int) -> subprocess.CompletedProcess`
- `cmd_version(data_dir: Path)`
  - Show version info.
- `cmd_update(data_dir: Path)`
  - Pull latest code, rebuild Docker, restart.

### `core/validator.py`

- `format_results(results: list[ValidationResult]) -> str`
  - Format validation results for Telegram.
- `validate_command(subcmd: str) -> str`
  - Entry point for Telegram /validate command.
- `RealWorldValidator._cfg(self) -> dict`
  - Load real config.yaml.
- `RealWorldValidator._api_call(self, provider: str, model: str, messages: list[dict], timeout: float) -> dict`
  - Make a real LLM API call.
- `RealWorldValidator.validate_config(self) -> ValidationResult`
  - Verify config.yaml is readable and has required keys.
- `RealWorldValidator.validate_deepseek_api(self) -> ValidationResult`
  - Make a real call to DeepSeek API.
- `RealWorldValidator.validate_minimax_api(self) -> ValidationResult`
  - Make a real call to MiniMax API.
- `RealWorldValidator.validate_evolve_logging(self) -> ValidationResult`
  - Write a real behavior log entry and verify it persists.
- `RealWorldValidator.validate_memory_write_read(self) -> ValidationResult`
  - Write to memory.jsonl and read it back.
- `RealWorldValidator.validate_telegram_bot(self) -> ValidationResult`
  - Verify Telegram bot token is set and bot info is fetchable.
- `RealWorldValidator.validate_disk_space(self) -> ValidationResult`
  - Check disk usage is under 95%.
- `RealWorldValidator.validate_git_repo(self) -> ValidationResult`
  - Verify git repo is clean and reachable.
- `RealWorldValidator.validate_scheduler_state(self) -> ValidationResult`
  - Verify scheduler can load its state file.
- `RealWorldValidator.validate_watchdog(self) -> ValidationResult`
  - Run a single watchdog health check.
- `RealWorldValidator.run_all(self) -> list[ValidationResult]`
  - Run every validation.

### `core/verdict_cache.py`

- `_char_bigrams(s: str) -> set[str]`
  - Extract character bigrams from a string, lowercased.

For Cantonese/English mix this captures word-shape similarity better
than word-level n-grams. Pure character-level would be too noisy
(every share
- `jaccard(a: str, b: str) -> float`
  - Jaccard similarity on character bigrams: |A ∩ B| / |A ∪ B|.
- `find_reusable_verdict(goal: str, tier: int) -> Optional[dict]`
  - Return a previous case dict if the goal is similar enough to a recent APPROVED case.

Returns None if no suitable case exists. The caller should treat the
returned dict as a "best prior verdict" refer

### `core/verifier.py`

- `verify_step(goal: str, tool_name: str, tool_args: dict, tool_result: str, config: dict, model_id: Optional[str]) -> dict`
  - Verify a single tool execution result.

Uses the LLM to score the result against the original goal.

Args:
    goal: The original user prompt / task description
    tool_name: Name of the tool that wa
- `_truncate(text: str, max_len: int) -> str`

### `core/watchdog.py`

- `mark_step_start(step_desc: str) -> float`
  - Record step start time for stuck detection.
- `mark_step_done(start_time: float) -> float`
  - Record step completion.
- `check_stuck(start_time: float) -> bool`
  - Check if a step has been running too long. Returns True if stuck.
- `kill_stuck_step(step_desc: str) -> dict`
  - Kill the current stuck step and return recovery instructions.
- `start_watchdog(interval_sec: int)`
  - Start a background thread that monitors BAW health.
- `track_consecutive_failure(tool_name: str) -> bool`
  - Track consecutive failures per tool. Returns True if loop detected (≥5 same-tool failures).

Resets on successful call via clear_consecutive_failures().
- `clear_consecutive_failures(tool_name: str | None)`
  - Reset failure counter for a tool (or all tools).
- `_recover_restart(reason: str) -> dict`
  - Gracefully restart the bot process with minimal disruption.

Returns recovery report dict.

### `tools/background.py`

- `start_bg(command: str, workdir: str | None) -> str`
  - Start a command in the background.

Args:
    command: Shell command to run.
    workdir: Working directory (default: current).

Returns:
    Confirmation with session ID.
- `output_bg(bg_id: str, clear: bool) -> str`
  - Get output from a background process.

Args:
    bg_id: Process ID (e.g., 'bg1').
    clear: If True, clear accumulated output after reading.

Returns:
    Formatted output with status.
- `stop_bg(bg_id: str) -> str`
  - Stop a background process.

Args:
    bg_id: Process ID to stop.

Returns:
    Confirmation.
- `_dispatcher(action: str, command: str, bg_id: str, workdir: str, clear: bool) -> str`
  - Dispatch background actions.

### `tools/bash.py`

- `_extract_paths(cmd: str) -> list[str]`
  - Extract file paths from a shell command.
- `_matches_glob(pattern: str, path: str) -> bool`
  - Simple glob matching for sensitive path patterns.
- `_is_sensitive(cmd: str) -> tuple[bool, str]`
  - Check if command tries to read sensitive files or execute dangerous operations.
- `bash(command: str, workdir: str | None, timeout: int) -> str`
  - Execute a shell command. Cross-platform (Linux + macOS).

### `tools/browser.py`

- `browser_navigate(url: str) -> str`
  - Navigate to a URL in the browser.
- `_stub(tool: str) -> str`

### `tools/code_scan.py`

- `_scan_file(path: Path) -> dict`
  - Scan a single file for security patterns.
- `_scan_directory(path: str, scan_type: str, max_files: int) -> str`
  - Scan a directory for security risks.

Args:
    path: Directory to scan.
    scan_type: "quick" (extensions only), "full" (all files), "deps_only" (requirements/pyproject only).
    max_files: Maximum
- `_build_summary(critical: int, high: int, total: int) -> str`
  - Build a human-readable summary.
- `_handler(path: str, scan_type: str, max_files: int) -> str`

### `tools/codebase_doc.py`

- `_is_baw_import(name: str) -> bool`
  - Return True if `name` looks like an import of another BAW module.
- `_relative_to_baw(path: Path) -> str`
  - Convert absolute path to BAW-relative like core/loop.py.
- `_extract_module_info(filepath: Path) -> dict[str, Any]`
  - Parse a single .py file and return structured module info.
- `_get_parent_chain(tree: ast.Module, target: ast.AST) -> list[ast.AST]`
  - Walk the tree and return parent chain for target node.
- `_extract_function_info(node: ast.FunctionDef) -> dict[str, Any]`
  - Extract info from a FunctionDef node.
- `_extract_class_info(node: ast.ClassDef, filepath: Path, source: str, lines: list[str]) -> dict[str, Any]`
  - Extract info from a ClassDef node.
- `_find_circular_dependencies(deps: dict[str, set[str]]) -> list[list[str]]`
  - Detect circular dependencies using DFS with path tracking.
- `_format_docstring(doc: str, max_len: int) -> str`
  - Truncate and clean a docstring for one-line display.
- `report(scan_data: Optional[dict]) -> str`
  - Generate a structured plain-text report of the codebase.

Modules scanned, dependency map, interface contracts, circular deps.
- `write_index(scan_data: Optional[dict]) -> str`
  - Write INDEX.md to BAW_HOME with complete module inventory and map.

This acts as a "readme for AI" — when BAW needs to modify its own
code, it first reads INDEX.md to understand the architecture.
- `codebase_doc(do_scan: bool, do_report: bool, do_write_index: bool, do_verify: bool, do_invalidate_cache: bool) -> str`
  - BAW codebase documentation tool.

Scans BAW's own codebase and produces structured documentation,
dependency maps, and import verification.

Args:
    do_scan: Force a fresh scan (default: load from c

### `tools/config.py`

- `_save_cfg(cfg: dict) -> str`
  - Save config dict to YAML file. Returns error string or empty on success.
- `_navigate(cfg: dict, path: str, create_missing: bool) -> tuple[dict | bool, str]`
  - Navigate dotted path like 'providers.minimax.base_url'.
Returns (parent_dict, last_key) or (False, error).
- `config_get(path: str) -> str`
  - Read a config value by dotted path.

Args:
    path: Dotted path, e.g. 'model.default', 'providers.minimax.base_url'

Returns:
    Formatted value or error.
- `config_set(path: str, value: str) -> str`
  - Set a config value by dotted path. Auto-validates YAML after write.

Creates a backup before writing. On validation failure, restores backup.

HARD GATE: If the path targets a protected setting (model
- `config_delete(path: str) -> str`
  - Delete a config key by dotted path.

Creates a backup before writing. On validation failure, restores backup.

HARD GATE: If the path targets a protected setting (model/provider/endpoint
etc.) and the
- `config_restore(backup_name: str) -> str`
  - Restore config from a backup.

Args:
    backup_name: Backup filename (e.g. 'config_20260615_120000.yaml').
                 If empty, restores latest backup.

Returns:
    Confirmation or error.
- `config_set_key(key: str, value: str) -> str`
  - Set an API key in ~/.baw/.env. Read-merge-write (never overwrites other keys).

HARD GATE: Rejects empty values. Use config_delete_key() to remove a key.
- `request_config_change(path: str, value: str, reason: str) -> str`
  - Propose a config change to the user for approval.

BAW uses this tool instead of silently changing model/provider/endpoint
settings via config_set().  The proposal is returned to the user; they
can ap
- `_dispatcher(action: str, path: str, value: str, backup_name: str, key_name: str, key_value: str, reason: str) -> str`

### `tools/cronjob.py`

- `_get_cron_dir(data_dir: str | None) -> Path`
- `_load_jobs(cron_dir: Path) -> list[dict]`
- `_save_jobs(cron_dir: Path, jobs: list[dict]) -> str`
- `_parse_schedule(schedule: str) -> str | None`
  - Validate schedule string and return None (valid) or error message.
- `_apply_tz(schedule: str, h: int, m: int, now_utc: datetime, for_weekly: tuple[int, int] | None) -> float`
  - Calculate next run in user's timezone, return UTC timestamp.

for_weekly: (target_dow, days_ahead_base) for weekly schedule.
- `_next_run(schedule: str) -> float`
  - Calculate next run timestamp from schedule string.
- `cronjob(action: str, schedule: str, prompt: str, name: str, data_dir: str | None) -> str`
  - Manage recurring cron jobs.

Args:
    action: create, list, remove, pause, resume, run.
    schedule: Interval syntax ("30m", "2h", "hourly", "daily@09:00", "weekly@sun@09:00").
    prompt: Task prom

### `tools/delegate_task.py`

- `_resolve_executor_model(cfg: dict, goal: str) -> str`
  - Resolve which model to use for a delegated task.

Priority:
1. model.task_rules — keyword match on goal (first match wins)
2. executor.model — configured executor model
3. model.fallback or model.defa
- `_get_minimax_config(goal: str, model_override: str) -> dict`
  - Load config and resolve the executor model (per-task routing support).
Falls back gracefully if resolved model is not in providers list.

Args:
    goal: Used to match model.task_rules for keyword-bas
- `delegate_task(goal: str, context: str, toolsets: str, model_id: str) -> str`
  - Delegate a task to a sub-agent (MiniMax executor).

Sub-agent runs independently with its own tools and isolated context.
Returns the sub-agent's final output.

Auto-fallback: if the chosen model erro

### `tools/docker.py`

- `_run(cmd: list[str], timeout: int, cwd: str | None) -> dict`
- `_docker_build(no_cache: bool) -> dict`
  - Docker mode: docker compose build + up -d.
- `_bare_build(no_cache: bool) -> dict`
  - Bare-metal mode: git pull + pip install -e . + systemctl restart.
- `_docker_logs(lines: int) -> dict`
- `_bare_logs(lines: int) -> dict`
- `_docker_cleanup(aggressive: bool) -> dict`
- `_bare_cleanup(aggressive: bool) -> dict`
- `_handler(action: str, no_cache: bool, aggressive: bool, lines: int) -> str`
  - Manage BAW lifecycle — works on Docker or bare-metal.

Auto-detects runtime mode. Same commands, transparent backend.

Args:
    action: 'status' | 'build' | 'restart' | 'logs' | 'cleanup'
    no_cach

### `tools/document_structuring.py`

- `_sanitize_filename(name: str) -> str`
- `_is_ignored(line: str) -> bool`
- `_extract_pdf_lines(file_path: str) -> list[dict]`
- `_extract_docx_lines(file_path: str) -> list[dict]`
- `_parse_into_chunks(extracted: list[dict], source_name: str) -> list[dict]`
- `parse_document(file_path: str) -> dict`
  - Parse a PDF or DOCX file, store chunks in the database.
- `get_toc(document_id: int) -> dict`
  - Get Table of Contents for a document.
- `get_chunk(chunk_id: int) -> dict`
  - Get full chunk content by ID.
- `search_chunks(query: str, document_id: int | None) -> dict`
  - Full-text search across chunks.
- `delete_document(document_id: int) -> dict`
  - Delete a document, its chunks, and physical files.
- `handler(action: str, file_path: str, document_id: int, chunk_id: int, query: str) -> Any`
  - Dispatch to the correct operation.
- `SectionNumberTracker.__init__(self, max_depth)`
- `SectionNumberTracker.sync(self, section_num: str)`
- `SectionNumberTracker.generate(self, level: int) -> str`

### `tools/execute_code.py`

- `execute_code(code: str, timeout: int) -> str`
  - Execute Python code with BAW tool access in an isolated scope.

Args:
    code: Python source code to execute.
    timeout: Max execution time in seconds (default 300).

Returns:
    Captured stdout +

### `tools/fusion_analyze.py`

- `_try_model(config: dict, provider_name: str, model_cfg: dict, question: str, results: list, lock: threading.Lock) -> None`
  - Try one model, append result to shared results list.
- `_synthesize(config: dict, question: str, raw_results: list[dict]) -> str`
  - Synthesize raw results into structured analysis using judge model.
- `fusion_analyze(question: str) -> str`
  - Run multi-model deliberation on a question using ALL configured providers.

Args:
    question: The question or topic to analyze.

Returns:
    Structured analysis with consensus, contradictions, insi
- `_handler(args: dict) -> str`

### `tools/get_skill.py`

- `get_skill(skill_name: str) -> str`
  - Get full content of a skill by name.

Skills live under ~/.baw/skills/. Each skill is a YAML file.
Returns the full content: description + steps + config + error handling.

Args:
    skill_name: Name 
- `_dispatcher(action: str, skill_name: str) -> str`
  - Dispatch get_skill actions.

### `tools/git.py`

- `_handler(action: str, message: str, count: int, branch: str) -> str`
  - Execute a git operation in the BAW repo.

Supported actions:
  status    — Show working tree status (modified/untracked files)
  log       — Show recent commits (use `count` param)
  diff      — Show 

### `tools/http_fetch.py`

- `detect_spa(html: str) -> bool`
  - Return True if the HTML response looks like a client-rendered SPA.

The fingerprint check is intentionally conservative — false positives
just mean "use browser fetch" which still works, but false neg
- `fetch_with_urllib(url: str, timeout: int) -> Tuple[str, str]`
  - Fetch a URL with stdlib urllib. Returns (status, body).
- `fetch_with_requests(url: str, timeout: int) -> Tuple[str, str]`
  - Fetch a URL with requests + BeautifulSoup. Returns (status, body).
- `http_fetch(url: str, prefer_browser: bool) -> Dict[str, Any]`
  - Fetch a URL with the right strategy for the page type.

Returns a dict with:
  - ``strategy``: "urllib" | "requests" | "BROWSER_REQUIRED" | "ERROR"
  - ``body``: raw HTML (urllib/requests) or empty wh
- `_suggest_mirror_path(url: str) -> str`
  - Suggest a stable mirror path under data/ for a URL.
- `read_mirror(mirror_path: str) -> Optional[str]`
  - Read a previously-mirrored file. Returns None if it doesn't exist.

Self-build tools should ALWAYS prefer ``read_mirror()`` over a fresh
network call — the mirror is the source of truth, the network i

### `tools/image_generate.py`

- `image_generate(prompt: str, aspect_ratio: str) -> str`
  - Generate an image from a text prompt using OpenAI DALL-E.

### `tools/install.py`

- `install(package: str, method: str, global_install: bool) -> str`
  - Install a package or CLI tool using the specified method.

Supports: npm, pip, apt, gem, go, cargo, etc.
Auto-detects method if not specified.

Args:
    package: Package name or 'name@version'
    me

### `tools/kg_curator.py`

- `_save(data: dict)`
- `curate(action: str, dry_run: bool) -> str`
  - Curate the KG: consolidate noise, retain signal.

Args:
    action: 'report' (default) | 'curate'
    dry_run: If True, only report what would be done

Returns:
    Summary of curation actions.
- `_handler(args: dict) -> str`

### `tools/knowledge_graph.py`

- `_save(data: dict)`
- `_normalize(s: str) -> str`
  - Normalize entity name for matching.
- `add_triple(subject: str, relation: str, object: str) -> str`
  - Add a fact: subject -[relation]-> object.

Auto-deduplicates: if identical triple exists, returns existing ID.
Auto-registers entities. Supports inverse relations.

Args:
    subject: The subject enti
- `query_entity(entity: str) -> str`
  - Find all triples involving this entity.

Args:
    entity: Entity name (fuzzy matched).

Returns:
    Formatted list of relations.
- `query_relation(relation: str) -> str`
  - Find all triples with this relation.

Args:
    relation: Relation to search (e.g., 'installed_via').

Returns:
    Formatted list of triples.
- `search_knowledge(query: str) -> str`
  - Full-text search through subjects and objects.

Args:
    query: Search text.

Returns:
    Formatted matching triples.
- `_extract_entities_from_content(content: str) -> list[str]`
  - Extract meaningful entities from plain-text memory content.

Returns up to 5 entity names detected from keywords.
- `_dispatcher(action: str, subject: str, relation: str, object: str, entity: str, query: str) -> str`
  - Dispatch knowledge graph actions.

Validates required params per action before calling.

### `tools/list_files.py`

- `_format_size(size_bytes: int) -> str`
  - Format file size in human-readable form.
- `_format_time(ts: float) -> str`
  - Format epoch timestamp to human-readable string.
- `list_files(path: str, pattern: str, sort_by: str, reverse: bool, limit: int, include_dirs: bool) -> str`
  - List files in a directory with metadata.

Args:
    path: Directory to list (default: current directory).
    pattern: Glob pattern to filter files (e.g., '*.jpg', 'photo_*').
    sort_by: Sort key — 

### `tools/mcp.py`

- `_load_mcp_config(config_path: str) -> dict`
  - Load MCP server configuration.
- `_dispatcher(action: str, server: str, tool: str, args: str, config_path: str) -> str`
  - Dispatch MCP actions.

'connect' — connect to all configured MCP servers (or a specific one)
'list_servers' — show connected servers
'list_tools' — show tools from a connected server
'call' — call an 
- `_list_tools(server: str) -> str`
  - List tools from a specific MCP server.

### `tools/memory.py`

- `memory_remember(content: str, tags: str, source: str) -> str`
  - Save a memory entry. Routes through curation gate for judgment.

The curator decides: save new, update existing, merge, or discard.
This prevents noise, ensures corrections update old entries, and
ass
- `memory_search(query: str, limit: int) -> str`
  - Search memory for relevant entries.

Args:
    query: Search query string.
    limit: Max results to return (default: 10).

Returns:
    Formatted list of matching memories.
- `memory_recall(limit: int) -> str`
  - Recall recent memories (most recent first).

Args:
    limit: Max entries to return (default: 20).

Returns:
    Formatted list of recent memories.
- `_memory_dispatcher(action: str, content: str, tags: str, limit: int) -> str`
  - Dispatch memory actions.

### `tools/memory_quality.py`

- `_kg_metrics(data: dict) -> tuple[int, int, int, float]`
  - Return (total, signal_count, noise_count, signal_ratio_pct).
- `_compute_loss(total_kg: int, noise_count: int, avg_score: float, edges: int) -> float`
  - Composite loss score. Lower = better.

Formula:
  noise_ratio * 0.5 + (1 - avg_memory_score) * 0.3 + fragmentation_penalty * 0.2

- noise_ratio = noise / total_kg (0-1, higher = worse)
- avg_memory_sc
- `_handler(args: dict) -> str`

### `tools/mmx.py`

- `_run_mmx(args: list[str], timeout: int) -> str`
  - Run mmx command with --output json and return parsed result.
- `mmx_text(messages: str, system: str, model: str) -> str`
  - Chat with MiniMax model via CLI.

Args:
    messages: Messages separated by newlines, or JSON array.
    system: Optional system prompt.
    model: Model name (default: MiniMax-M2.7-highspeed).

Retur
- `mmx_image(prompt: str, n: int, aspect_ratio: str, out_dir: str) -> str`
  - Generate an image via MiniMax.

Args:
    prompt: Image description.
    n: Number of images (1-4).
    aspect_ratio: e.g. '1:1', '16:9', '9:16', '4:3', '3:4'.
    out_dir: Output directory (default: 
- `mmx_speech(text: str, voice: str, speed: float, out_path: str) -> str`
  - Generate speech via MiniMax TTS.

Args:
    text: Text to synthesize.
    voice: Voice name (default: auto-detect Cantonese).
    speed: Speed multiplier (0.5-2.0).
    out_path: Output file path.

Re
- `mmx_video(prompt: str, download: bool, out_path: str) -> str`
  - Generate a video via MiniMax.

Args:
    prompt: Video description.
    download: If True, download the video (may take time).
    out_path: Output file path.

Returns:
    JSON with video task info, 
- `mmx_music(prompt: str, lyrics: str, instrumental: bool, lyrics_optimizer: bool, out_path: str) -> str`
  - Generate music via MiniMax.

Args:
    prompt: Music description (e.g. 'Upbeat pop', 'Cinematic orchestral').
    lyrics: Optional lyrics text.
    instrumental: If True, no vocals.
    lyrics_optimiz
- `mmx_vision(image_path: str, prompt: str) -> str`
  - Analyze an image via MiniMax Vision.

Args:
    image_path: Path or URL to image.
    prompt: Optional question about the image.

Returns:
    Description/analysis text.
- `mmx_search(query: str) -> str`
  - Search the web via MiniMax Search.

Args:
    query: Search query.

Returns:
    Search results.
- `_dispatcher(command: str, prompt: str, text: str, voice: str, speed: float, image_path: str, query: str, n: int, aspect_ratio: str, lyrics: str, instrumental: bool, lyrics_optimizer: bool, out_path: str, download: bool, messages: str, system: str, model: str) -> str`
  - Dispatch mmx commands.

### `tools/patch.py`

- `patch_file(path: str, old_string: str, new_string: str, replace_all: bool) -> str`
  - Replace a string in a file with another string.

Args:
    path: Path to the file to edit.
    old_string: Exact text to find and replace. Must be unique unless replace_all=True.
    new_string: Repla

### `tools/petrestaurants.py`

- `_parse_source_md(md_text: str) -> list[dict]`
  - Extract restaurants from the mirrored markdown file.

The source structure is:
  ### <region>          ← region header
  | name | district | address |
  | name | district | address |
  | ... |
  ### <
- `_build_dataset(rows: list[dict], source_url: str) -> dict`
- `_haversine_km(lat1: float, lon1: float, lat2: float, lon2: float) -> float`
  - Great-circle distance in km between two lat/lon points.
- `_format(r: dict, extra: Optional[dict]) -> str`
- `search_by_district(district: str) -> str`
  - Filter restaurants by district name (e.g. 灣仔, 油尖區, 離島).
- `search_by_region(region: str) -> str`
  - Filter by region: 港島區 / 九龍區 / 新界區.
- `nearest(lat: float, lon: float, k: int) -> str`
  - Return the k nearest pet-friendly restaurants to a lat/lon point.
- `search(query: str, region: Optional[str], district: Optional[str], k: int) -> str`
  - Fuzzy name search with optional region/district filter.
- `_dispatch(action: str) -> str`

### `tools/read_file.py`

- `_is_sensitive(path: str) -> tuple[bool, str]`
- `read_file(path: str, offset: int, limit: int) -> str`
  - Read a text file with line numbers.

### `tools/remember.py`

- `remember_fact(fact: str, category: str) -> str`
  - Save a quick fact/note. Routes through curation gate.

Args:
    fact: What to remember (keep concise — max 500 chars).
    category: Category for grouping (e.g., 'install', 'config', 'bug', 'discover
- `recall_facts(category: str, limit: int) -> str`
  - Recall recent facts, optionally filtered by category.

Args:
    category: Optional — only show facts from this category.
    limit: Max entries to return (default: 10).

Returns:
    Formatted list o
- `forget_fact(index: int) -> str`
  - Remove a fact by index (1-based, from most recent).

Args:
    index: 1-based index from most recent. -1 = delete all.

Returns:
    Result message.
- `_dispatcher(action: str, fact: str, category: str, limit: int, index: int) -> str`
  - Dispatch remember actions.

### `tools/resource_monitor.py`

- `_run(cmd: list[str], timeout: int) -> dict`
- `_size_mb(path: Path) -> float`
  - Get total size of a path in MB.
- `_cleanup(aggressive: bool) -> dict`
  - Clean up stale sessions, old logs, and temp files.
- `_handler(action: str, aggressive: bool) -> str`
  - Monitor and manage BAW's resource usage.

Supported actions:
  report    — Show disk usage report for all BAW data directories
  cleanup   — Clean stale sessions, old logs, temp files
  stale     — Li

### `tools/restaurant.py`

- `_bbox_query(south: float, west: float, north: float, east: float, amenity: str, limit: int) -> str`
  - Return an Overpass QL query string for nodes+ways in a bbox.
- `_run_overpass(query: str) -> List[Dict[str, Any]]`
  - POST query to Overpass, return list of normalized POI dicts.

Each result dict:
    { id, name, lat, lon, amenity, cuisine, tags }
- `_format_addr(tags: dict) -> str`
- `_cache_key(query: str) -> str`
- `_cache_get(query: str) -> Optional[List[Dict[str, Any]]]`
- `_cache_put(query: str, results: List[Dict[str, Any]]) -> None`
- `_haversine_km(lat1: float, lon1: float, lat2: float, lon2: float) -> float`

### `tools/scan_and_adopt.py`

- `_sanitize_name(name: str) -> str`
  - Sanitize a name for use as a Python module name and file path.

Rejects: path traversal (.., /, \), empty, reserved names.
Keeps only: alphanumeric + underscore + hyphen (hyphens → underscores).
- `_run(cmd: list[str], timeout: int, cwd: str | None) -> dict`
- `_scan_directory(path: str, depth: int) -> list[dict]`
  - Scan a directory for discoverable tools/skills.

Recognizes:
- Python files with TOOL_DEF (BAW format)
- SKILL.md files (Hermes format)
- *.py files with register() calls
- *.sh files
- `_analyze_via_llm(code: str, name: str, source_type: str) -> str`
  - Use LLM to analyze foreign code and generate BAW tool code.
- `_safety_check(tool_code: str) -> list[str]`
  - Scan generated tool code for dangerous patterns. Returns list of findings.
- `_adopt_file(entry: dict) -> dict`
  - Adopt a single foreign tool/skill into BAW.
- `_register_tool(name: str)`
  - Add import + register to __init__.py.
- `_download_git(url: str) -> dict`
  - git clone a repo into a temp dir. Returns dict with ok, path, tmpdir.
- `_handler(source: str, target_type: str, dry_run: bool) -> str`
  - Discover and adopt external tools/skills into BAW.

Scans a target (directory path, git URL, or pip package) for tools/skills
from other systems, analyzes them, and generates BAW-compatible versions.


### `tools/search_files.py`

- `search_files(pattern: str, path: str, file_glob: str, limit: int) -> str`
  - Search file contents or find files by name.

Args:
    pattern: Regex pattern for content search, or glob for file search.
    path: Directory to search in (default: current working directory).
    fi
- `_rg_search(root: str, pattern: str, file_glob: str, limit: int) -> str`
  - Search using ripgrep.
- `_py_search(root: str, pattern: str, file_glob: str, limit: int) -> str`
  - Fallback: search using Python standard library.

### `tools/self_diagnose.py`

- `_run(cmd: list[str], timeout: int) -> dict`
- `_handler(quick: bool, fix: bool) -> str`
  - Run comprehensive self-diagnosis.

Checks: container health, LLM providers, registered tools,
memory store, disk usage, config integrity, cron jobs.

Args:
    quick: If True, skip provider checks (fa
- `_get_recommendations(checks: dict) -> list[str]`
  - Generate recommendations based on check results.

### `tools/self_discover.py`

- `_suggest_new_tools(gaps: dict, failures: list[dict]) -> list[dict]`
  - Generate tool suggestions based on gaps and failures.
- `_handler(action: str, auto_fix: bool) -> str`
  - Analyze BAW's capabilities and detect gaps.

Scans:
- Current tool inventory (what tools exist)
- Recent failure logs (what went wrong)
- Domain coverage (which capability domains are missing)

Propos
- `_calc_health(inventory: dict, gaps: dict, failures: list) -> str`
  - Calculate a simple health score.

### `tools/self_migrate.py`

- `_run(cmd: list[str], timeout: int) -> dict`
- `_export_data(target_dir: Path) -> dict`
  - Export BAW data (config, memory, sessions, SOUL) to a tar.gz.
- `_generate_bootstrap(target_dir: Path) -> dict`
  - Generate a bootstrap script for the target machine.
- `_analyze_target(host: str, username: str) -> dict`
  - Analyze target machine (or local environment).
- `_handler(action: str, target_dir: str, host: str, username: str) -> str`
  - Prepare and execute BAW migration to a new machine.

Supported actions:
  analyze     — Analyze target machine (local or remote via SSH)
  export      — Export BAW data (config, memory, sessions, SOUL
- `_full_migrate(target_dir: Path, host: str, username: str) -> dict`
  - Run full migration: analyze → export → bootstrap.

### `tools/selftest.py`

- `selftest(full: bool) -> str`
  - Run BAW self-test suite.

Args:
    full: If True, also runs API connectivity checks (slower, may incur cost). Default False.

Returns:
    Markdown report of test results.

### `tools/session_search.py`

- `_get_db(data_dir: Path) -> sqlite3.Connection`
  - Get or create FTS5 database connection for session search.
- `_index_session(conn: sqlite3.Connection, path: Path, meta: dict) -> bool`
  - Index a single session file. Returns True if new data was indexed.

Uses mtime tracking to avoid re-indexing unchanged files.
- `session_search(query: str, limit: int, data_dir: str | None) -> str`
  - Search past sessions using FTS5 full-text search.

Args:
    query: FTS5 query string (keywords, phrases in FTS5 syntax).
    limit: Max results to return (default 5, max 20).
    data_dir: BAW data d

### `tools/session_synthesis.py`

- `_keywords(text: str) -> set[str]`
  - Extract meaningful keywords from text (mirrors MemoryStore._keywords).
- `_jaccard(a: set, b: set) -> float`
  - Jaccard similarity between two keyword sets.
- `_entries_since(ms: MemoryStore, days_back: int) -> list[dict]`
  - Return entries created within the last N days.

Scans the internal cache to avoid search-query limitations.
- `_cluster_entries(entries: list[dict], threshold: float) -> list[list[dict]]`
  - Group entries by keyword similarity (Jaccard >= threshold).
- `_cluster_topic(entries: list[dict]) -> str`
  - Derive a topic name from the most common keyword among cluster entries.
- `_cluster_sentiment(entries: list[dict]) -> str`
  - Detect whether the cluster is correction/confirmation/neutral.
- `_days_spread(entries: list[dict]) -> int`
  - Count how many distinct days the entries span.
- `_cross_ref(cluster_kw: set[str], kg_topics: dict) -> dict`
  - Check which KG triples relate to cluster keywords.

Returns dict with keys: count, signal_count, reference_count.
- `_generate_insights(clusters: list[list[dict]], total_entries: int, kg_topics: dict) -> list[dict]`
  - Generate insights from clusters.

Returns list of dict: {text, type, cluster, topic, score}.
- `synthesize(days_back: int, store: bool) -> str`
  - Run cross-session synthesis on MemoryStore memories.

Args:
    days_back: How many days of memory to analyze (default 7).
    store: Whether to store top 3 insights back into memory (default True).


- `_handler(args: dict) -> str`

### `tools/skill_import.py`

- `_sanitize_name(name: str) -> str`
  - Sanitize a name for use as a Python module name and file path.
- `_parse_hermes_skill(content: str) -> dict`
  - Parse a Hermes skill.md file into structured data.
- `_convert_to_tool_code(skill: dict) -> str`
  - Convert parsed skill data into BAW tool Python code.

Uses repr() for all user-controlled content to prevent injection
through triple-quotes or other special characters.
- `_import_file(path: str) -> dict`
  - Import a single skill file into BAW.
- `_register_tool(name: str)`
  - Add import + register to __init__.py.
- `_handler(source: str, dry_run: bool) -> str`
  - Import skills from other systems into BAW tools.

Reads skill files (Hermes SKILL.md format, or any markdown with YAML frontmatter),
parses them, and converts to BAW-compatible tools.

Args:
    sourc

### `tools/system.py`

- `_run(cmd: list[str], timeout: int) -> dict`
  - Run a shell command and return {ok, output, error}.
- `_handler(action: str, name: str, log_lines: int) -> str`
  - Manage BAW's runtime system.

Supported actions:
  status        — Show overall system health (services, cron, disk, uptime)
  services      — List BAW systemd services with status
  restart       — R

### `tools/todo.py`

- `set_session(session_id: str) -> None`
  - Reset the bound state when the session id changes (called by loop boot).
- `_format_items(items: list, stats: dict | None) -> str`
- `todo_write(todos_json: str, merge: bool) -> str`
  - Replace/merge the task portion of the list.

Kept for back-compat with old callers. Use add_thought/add_followup for the
new types.
- `todo_read(active_only: bool) -> str`
- `todo_add_thought(content: str, note: str) -> str`
  - Capture a self-reflection / idea. Always visible until explicitly removed.
- `todo_add_followup(content: str, note: str) -> str`
  - Schedule an action for a future turn/session. Will surface at next boot.
- `todo_done(item_id: str) -> str`
- `todo_cancel(item_id: str) -> str`
- `todo_remove(item_id: str) -> str`
- `_resolve_id(st: TodoState, short_or_full: str) -> str | None`
  - Accept either a full id or the last-6-chars suffix.
- `_todo_dispatcher(action: str) -> str`

### `tools/tool_generate.py`

- `_syntax_check(path: Path) -> dict`
  - Check Python syntax of a file.
- `_generate_code(name: str, description: str, what_it_does: str) -> str`
  - Use LLM to generate tool code.
- `_write_code(name: str, code: str) -> dict`
  - Write generated code to tools/<name>.py.
- `_update_init(name: str) -> dict`
  - Add tool import and registration to tools/__init__.py.
- `_smoke_test(name: str) -> dict`
  - Run a simple smoke test: import the tool.
- `_handler(name: str, description: str, what_it_does: str, skip_generate: bool, code: str) -> str`
  - Generate a new BAW tool from a prompt.

Steps:
1. LLM generates tool code based on description
2. Writes code to tools/<name>.py
3. Updates tools/__init__.py with import + registration
4. Syntax check

### `tools/tts.py`

- `_get_env(key: str) -> str`
- `tts_list_voices(provider: str) -> str`
  - List voices for the current/default provider.
- `_tts_minimax(text: str, voice: str, output_path: str, speed: float) -> str`
  - MiniMax TTS — reads base_url from config.yaml so international endpoints work.
- `_tts_stepfun(text: str, voice: str, output_path: str, speed: float) -> str`
  - Stepfun TTS via OpenAI-compatible /v1/audio/speech (step_plan for monthly)
- `_tts_edge(text: str, voice: str, output_path: str, speed: float) -> str`
  - Edge TTS (local, free) — requires pip install edge-tts
- `tts_generate(text: str, voice: str, output_path: str, speed: float, provider: str) -> str`
  - Generate TTS audio using the configured or detected provider.

Args:
    text: Text to convert to speech
    voice: Voice ID. Override to use a specific voice.
    output_path: Output mp3 path. Defaul

### `tools/vision.py`

- `_encode_image(path: str) -> str`
  - Encode image to base64 data URI.
- `_vision_minimax(path: str, question: str) -> str`
  - Direct MiniMax vision API call (OpenAI-compatible).
- `_vision_stepfun(path: str, question: str) -> str`
  - Use Stepfun multimodal API as fallback.
- `_vision_mmx_cli(path: str, question: str) -> str`
  - Legacy mmx CLI call (if available).
- `vision_describe(path: str, question: str) -> str`
  - Describe an image using vision AI (MiniMax API primary, Stepfun fallback).

Args:
    path: Path to the image file
    question: Optional question about the image

Returns:
    Description from vision

### `tools/web_extract.py`

- `web_extract(url: str, strip_lines: bool, max_chars: int) -> str`
  - Fetch a web page and extract plain text content using requests + BeautifulSoup.

Strips scripts, styles, and excessive whitespace. Returns readable plain text.

Args:
    url: Full URL to fetch (http/

### `tools/web_search.py`

- `web_search(query: str, limit: int, provider: str | None) -> str`
  - Search the web using BAW's search provider registry.

Args:
    query: Search query string
    limit: Max results (default: 5)
    provider: Provider name (optional, uses config default)

Returns:
   

### `tools/write_file.py`

- `write_file(path: str, content: str) -> str`
  - Write content to a file. Creates parent directories if needed.

---
_Generated at 2026-06-20 17:27:53.990553_
