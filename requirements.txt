# BAW Bot — core dependencies (Docker)
# No Hermes venv dependency. Self-contained.

httpx>=0.28
requests>=2.30
PyYAML>=6.0
openai>=2.0
beautifulsoup4>=4.12
croniter>=6.0
pygments>=2.17
python-dotenv>=1.0
tavily-python>=0.5

# ── CLI / Dashboard UI ──
rich>=13.0
textual>=0.52
