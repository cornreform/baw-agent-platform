# BAW Bot — core dependencies (Docker)
# Self-contained — no external agent dependency.

httpx>=0.28
requests>=2.30
PyYAML>=6.0
openai>=2.0
beautifulsoup4>=4.12
croniter>=6.0
html2text>=2024
pygments>=2.17
python-dotenv>=1.0

# ── Document processing (install-time, not runtime discovery) ──
python-docx>=1.1
# ── STT — Speech-to-text ──
edge-tts>=6.0  # TTS fallback

# ── WebSocket — Slack Socket Mode ──
websocket-client>=1.7

# ── CLI / Dashboard UI ──
rich>=13.0

# ── MCP — Model Context Protocol ──
mcp>=1.0.0
