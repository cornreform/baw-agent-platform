# BAW Self-Audit Report — 2026-06-25 01:30

## Score: 81.4% (B)

### ✅ Working
- 6 providers: all health OK
- 47 tools registered, 129 modules clean
- Disk: 7.2G/235G (4%) healthy
- Config valid, API keys loaded
- Telegram connector active, HTML parse_mode

### 🔴 HIGH Priority
1. **tools.mcp broken import** — Py3.9 event loop in thread (needs `asyncio.set_event_loop`)
2. **STT config contradictory** — `method: hybrid` + `base_url` set → fix to `method: auto-asr` or consistent
3. **output.format not in config.yaml** — need explicit config

### 🟡 MEDIUM Priority
4. **Service lifecycle** — shows "stopped" but process running (cosmetic)
5. **Memory tools wrong signature** — `action` parameter issue in memory_quality + kg_curator
6. **kg_curator known bug** — identified earlier, needs fix

### 🟢 LOW Priority
7. **TTS** — edge-tts fallback, consider MiniMax for Cantonese
8. **No cron jobs** — add daily health check
9. **security feature** — .env read blocked (by design)
10. **Docker CLI** — not available in BAW context (fine for bare metal)
